# Interop generator notes
Generated by `carrier/gen/gen_interop.py` from `artifacts/dwarf_info.txt` + `artifacts/functions.json`, scope=`game`.
## Counts (KNOWN, from this run)
| item | count |
|---|---:|
| structs_unions | 70 |
| enums | 0 |
| typedefs | 109 |
| globals | 156 |
| functions | 253 |

CU scope: 25 compile units in scope out of 148 total in the DWARF.

## Calling convention detection (KNOWN)
DWARF carries no `DW_AT_calling_convention` anywhere in this dump (checked). Convention is instead read from COFF symbol table decoration (`carrier/gen`'s `--functions` sibling `coff_symbols.json`, or defaults to cdecl if no COFF symbol matches the VA): a MinGW `__stdcall` symbol is decorated `_name@N`; a plain `_name` is `__cdecl`. Verified on `create_post` (`_create_post`, cdecl) and `_WinMain@16` (stdcall).
1 of 253 emitted functions detected as `__stdcall`.

Embedded function-pointer *type* members (e.g. Allegro driver vtables) always render as `__cdecl` (INFERRED): DWARF subroutine_type DIEs used only as pointer targets have no associated COFF symbol to check, so this is a default, not a measurement. Most reachable examples are Allegro internal method tables, which are cdecl by convention (`AL_METHOD`); Win32 callback typedefs (WNDPROC etc.) would need `__stdcall` and are NOT auto-detected -- flag if one is found broken.

## long double substitution (KNOWN)
Not reachable in this scope (`long double` appears in the DWARF file overall but no in-scope function/global in this run referenced it).

## Opaque / unrepresentable types (4)
- `BITMAP`: array member with unknown bound (C flexible array, DWARF contributes 0 bytes to the struct)
- `RLE_SPRITE`: array member with unknown bound (C flexible array, DWARF contributes 0 bytes to the struct)
- `FONT_GLYPH`: array member with unknown bound (C flexible array, DWARF contributes 0 bytes to the struct)
- `pthread_mutex_t_`: no DW_AT_byte_size (declaration-only, never fully defined in scope)

## Struct/union/typedef name conflicts across CUs (6)
- `DW_TAG_structure_type DIDEVCAPS`: canonical size 24 at offset 0x8d099 conflicts with size 44 at offset 0xd9d90 (kept the canonical one; DIEs likely describe genuinely different types that happen to share a tag name)
- `DW_TAG_structure_type DIDEVICEOBJECTINSTANCEA`: canonical size 292 at offset 0x8d270 conflicts with size 316 at offset 0xd9ffd (kept the canonical one; DIEs likely describe genuinely different types that happen to share a tag name)
- `DW_TAG_typedef DIDEVICEOBJECTINSTANCEA`: typedef targets are both DW_TAG_structure_type but disagree on byte_size (292 vs 316) (canonical offset 0x8d2e7, conflicting offset 0xda14f)
- `DW_TAG_structure_type DIDEVICEINSTANCEA`: canonical size 560 at offset 0x8d543 conflicts with size 580 at offset 0xda41e (kept the canonical one; DIEs likely describe genuinely different types that happen to share a tag name)
- `DW_TAG_typedef DIDEVICEINSTANCEA`: typedef targets are both DW_TAG_structure_type but disagree on byte_size (560 vs 580) (canonical offset 0x8d5d5, conflicting offset 0xda4ec)
- `DW_TAG_typedef BITMAPINFOHEADER`: typedef targets are both DW_TAG_structure_type but disagree on byte_size (16 vs 40) (canonical offset 0xc2f70, conflicting offset 0x17300f)

## Name collisions (disambiguated with `__<cu-basename>`)
Functions: 0 collisions. Globals: 0 collisions.

## Topological-order anomalies (0)
None -- no by-value/typedef dependency cycles found (expected for valid C).

## Bitfields
None found anywhere in the DWARF dump (`DW_AT_bit_size` occurs 0 times); no bitfield support was implemented.

## Compile / check results
Not run by this invocation. To verify (32-bit MSVC):

```
"C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Auxiliary\Build\vcvars32.bat"
cd carrier/gen
cl /nologo /c /W3 /TC it_selftest.c
cl /nologo /W3 /TC it_types_check.c /Fe:it_types_check.exe
it_types_check.exe
```
`it_selftest.c` must produce zero errors (its `IT_CT_ASSERT` lines are compile-time sizeof/offsetof checks -- any mismatch is a hard compile error, not a warning). `it_types_check.exe` must print `ALL PASS`. See the repo history / prior run notes for the last verified result on this exact output.
