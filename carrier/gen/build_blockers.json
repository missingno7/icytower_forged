{
  "_purpose": "Hand-curated DATA (not code): src/icytower/*.c files that scan_src_defs.py's --list-build-files would otherwise mechanically add to build.cmd's compile lists (they define a real, interop_index.json-recognized game function) but that do NOT actually compile-and-link cleanly into the carrier today, for a reason that has nothing to do with the MSVC-vs-GCC-x87 split --list-build-files itself decides. Consulted by scan_src_defs.py (excluded from both the msvc and gcc lists, with a warning) and by gen_bind_table.py (no binding-table row is generated for a function whose only defining file is blocked here, since there would be nothing linked for the row to point at). Remove an entry the day its file's missing symbols are wired up (asset-seam integration, tracked separately - see src/icytower/ASSETS.md) and it will pick itself back up automatically on the next build, exactly like any other src/icytower/*.c file.",
  "files": {
    "draw_buffer.c": "MEASURED this pass ('binding table generated', carrier/NOTES.md): cl reports LNK2019 unresolved externals _makecol, _textprintf_ex, _asset_font referenced by _draw_buffer. draw_buffer() calls real Allegro drawing primitives (makecol/textprintf_ex) and the asset-seam's font lookup (asset_font) - none of these are declared/resolved by carrier/gen/pf_bindings_src.h (game-global/game-function macros only) the way the standalone build's own Allegro headers + asset system resolve them (src/icytower/ASSETS.md). Wiring the carrier's src/ compile step to see Allegro's real API + the asset seam is a separate, not-yet-done integration task, not a binding-table concern; flagged here rather than guessed at.",
    "start_reward.c": "MEASURED this pass: cl reports LNK2019 unresolved external _asset_bitmap referenced by _start_reward (plus a C4047 BITMAP*/int mismatch warning at the same call site - the carrier world has no declared prototype for asset_bitmap() to check against). Same missing asset-seam wiring as draw_buffer.c above; src/icytower/PROMOTIONS.md's own batch-6 'Skipped this pass' entry for start_reward already flagged its asset-seam dependency (a computed-index DATAFILE lookup) as unmapped."
  }
}
