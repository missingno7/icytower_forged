# icytower15.exe Win32 Import / API Census

Target: `D:\Games\DOS\dos_recosystem\icytower_forged\assets\icytower15.exe` (Icy Tower 1.5.1, PE32 @ ImageBase 0x400000, MinGW GCC 4.4.1, statically linked Allegro 4.4 + its Windows drivers, statically linked libvorbis/libogg, full DWARF + COFF symbols present, dynamically linked libpng3.dll/pthreadGC2.dll/zlib1.dll).

Method (all claims below are marked KNOWN or INFERRED; see per-row rationale):

- Import table dumped previously to `imports.json` (320 entries, [dll, name, IAT slot VA]).
- Full linear disassembly generated with `objdump -d assets/icytower15.exe` -> `artifacts/icytower_disasm.txt` (238,295 lines).
- Call-site attribution: parsed every `call *0x51xxxx` / `jmp *0x51xxxx` (direct IAT-slot indirect call -- the common MinGW dllimport pattern) **and** every named-thunk call (`call ADDR <_Name@N>` where `ADDR` is a single-instruction `jmp *0x51xxxx` thunk -- the pattern used for imports linked without `__declspec(dllimport)`, e.g. most of msvcrt.dll and libpng3.dll). A third, rarer two-instruction thunk idiom (`mov 0x51xxxx,%eax ; ... ; jmp/call *%eax`) was found and handled specially for `atexit`/`_onexit` (MinGW's own CRT-startup wrapper functions of the same name forward to the real import this way).
- Caller -> source-CU attribution: primarily DWARF `.debug_info` compile-unit `low_pc`/`high_pc` ranges (full source paths, e.g. `F:\projects\icytower\trunk\source\main.c` vs `C:\Lib\allegro4\src\win\wddraw.c` -- this is what correctly separates the game's own `timer.c`/`main.c` from Allegro's own files of the same base name). Functions outside DWARF coverage (CRT/mingwex glue, libvorbis/libogg object files compiled without `-g`) fall back to the COFF symbol table's per-function `.file` aux entry.
- 320/320 imports were attributed to at least one call site or direct data reference except `msvcrt.dll!ftell`, which has **zero** static references anywhere in the disassembly (dead/unused import -- INFERRED reachable only via a code path not present, or vestigial from a statically-linked library routine that itself is never called).

## Summary

Total imports: **320**

### By class

| class | count | % of total |
|---|---:|---:|
| DIRECT | 123 | 38.4% |
| WRAP | 162 | 50.6% |
| DETERMINISTIC | 31 | 9.7% |
| SHIM | 4 | 1.2% |

**DIRECT + WRAP = 285 / 320 = 89.1%** of all imports need no behavioral reimplementation at all -- straight host forward (DIRECT) or forward-through-a-thin-tracing-shim (WRAP). Only the DETERMINISTIC set (31 imports, 9.7%) needs real interception logic, and only the SHIM set (4 imports, 1.3%: the DirectX factory functions) needs actual reimplementation/replacement.

### By DLL (with class breakdown)

| dll | total | DIRECT | WRAP | DETERMINISTIC | SHIM |
|---|---:|---:|---:|---:|---:|
| DDRAW.dll | 1 | 0 | 0 | 0 | 1 |
| dinput.dll | 1 | 0 | 0 | 0 | 1 |
| dsound.dll | 2 | 0 | 0 | 0 | 2 |
| GDI32.dll | 21 | 11 | 10 | 0 | 0 |
| KERNEL32.dll | 43 | 17 | 21 | 5 | 0 |
| libpng3.dll | 35 | 35 | 0 | 0 | 0 |
| msvcrt.dll | 114 | 58 | 48 | 8 | 0 |
| OLE32.dll | 3 | 0 | 3 | 0 | 0 |
| pthreadGC2.dll | 3 | 0 | 2 | 1 | 0 |
| SHELL32.DLL | 1 | 0 | 1 | 0 | 0 |
| USER32.dll | 56 | 2 | 43 | 11 | 0 |
| WINMM.DLL | 30 | 0 | 28 | 2 | 0 |
| WSOCK32.DLL | 10 | 0 | 6 | 4 | 0 |

Callback-bearing imports (receive a function pointer into guest/Allegro code, or into libpng's I/O shims): **17** -- listed individually in the table below (callback column) and summarized in the COM/callback section.

## Full import table

`callers` lists up to 4 distinct calling functions found in the disassembly, tagged by origin (`game` = icytower's own source under `F:\projects\icytower\trunk\source\`, `allegro` = statically-linked Allegro 4.4 under `C:\Lib\allegro4\`, `crt` = MinGW/mingwex/libgcc CRT glue, `vorbis/ogg` = statically-linked libvorbis/libogg). All caller attributions are KNOWN (read from the disassembly + DWARF/COFF symbol tables), not inferred.

| dll | import | class | cb? | #sites | callers | rationale |
|---|---|---|:-:|---:|---|---|
| DDRAW.dll | DirectDrawCreate | SHIM |  | 1 | _init_directx (allegro) | Legacy DirectDraw COM factory (Allegro wddraw.c _init_directx). cnc-ddraw's ddraw.dll is already installed in assets/ as a drop-in replacement for this exact DLL -- first option is 'do nothing', the existing PATH/side-by-side ddraw.dll shim already intercepts this. If not using cnc-ddraw, forwarding to host's real ddraw.dll works on Win11 (DirectDraw is still present) but HW accel/exclusive-mode behavior differs; the returned IDirectDraw2/Surface COM object is the real boundary (see COM section). |
| dinput.dll | DirectInputCreateA | SHIM |  | 3 | _mouse_directx_init (allegro); _key_directx_init (allegro); _joystick_dinput_init (allegro) | Legacy DirectInput7 factory (wmouse.c/wkeybd.c/wjoydx.c _*_directx_init). Windows 11 still ships dinput.dll for compatibility so straight host-forward is plausible; cnc-ddraw does not shim DirectInput so this would bypass it. Real work happens through the returned IDirectInputDevice(2) vtable (see COM section). |
| dsound.dll | DirectSoundCreate | SHIM |  | 4 | _digi_directsound_init (allegro); _digi_directsound_detect (allegro); _digi_dsoundmix_detect (allegro); _digi_dsoundmix_init (allegro) | Legacy DirectSound factory (wdsound.c/wdsndmix.c _digi_directsound*_init/detect). Win11 keeps dsound.dll for compatibility (software emulation via WASAPI under the hood) so host-forwarding is the pragmatic first option; cnc-ddraw does not shim DirectSound. Real buffer I/O is through IDirectSoundBuffer vtable calls (COM section). |
| dsound.dll | DirectSoundEnumerateA | SHIM | Y | 1 | __get_win_digi_driver_list (allegro) | Enumerates DirectSound devices via an LPDSENUMCALLBACKA passed as a parameter -- this is a genuine import-level callback (not just a COM vtable one). Callback target here is Allegro's own internal enumerator (__get_win_digi_driver_list), not game code, so the re-entrancy risk for a carrier is Allegro-internal, not guest-game. |
| GDI32.dll | BitBlt | DIRECT |  | 1 | _update_colorconv_window (allegro) | Pure draw/query operation on an already-open DC/bitmap; no new handle is created and the operation is deterministic given deterministic pixel/palette input. Used by Allegro's GDI-mode (non-DirectDraw) software blit fallback (wgdi.c/gdi.c). |
| GDI32.dll | CreateBitmap | WRAP |  | 1 | _gfx_directx_set_mouse_sprite (allegro) | Creates a GDI object handle (bitmap/DC/palette/brush). Handle identity/lifetime is exactly the kind of host-resource boundary WRAP exists for, even though the call itself is not observability-critical for gameplay. |
| GDI32.dll | CreateCompatibleBitmap | WRAP |  | 2 | _gfx_directx_set_mouse_sprite (allegro); _stretch_blit_from_hdc (allegro) | Creates a GDI object handle (bitmap/DC/palette/brush). Handle identity/lifetime is exactly the kind of host-resource boundary WRAP exists for, even though the call itself is not observability-critical for gameplay. |
| GDI32.dll | CreateCompatibleDC | WRAP |  | 3 | _gfx_directx_set_mouse_sprite (allegro); _stretch_blit_from_hdc (allegro) | Creates a GDI object handle (bitmap/DC/palette/brush). Handle identity/lifetime is exactly the kind of host-resource boundary WRAP exists for, even though the call itself is not observability-critical for gameplay. |
| GDI32.dll | CreateDIBitmap | WRAP |  | 1 | _convert_bitmap_to_hbitmap (allegro) | Creates a GDI object handle (bitmap/DC/palette/brush). Handle identity/lifetime is exactly the kind of host-resource boundary WRAP exists for, even though the call itself is not observability-critical for gameplay. |
| GDI32.dll | CreatePalette | WRAP |  | 1 | _convert_palette_to_hpalette (allegro) | Creates a GDI object handle (bitmap/DC/palette/brush). Handle identity/lifetime is exactly the kind of host-resource boundary WRAP exists for, even though the call itself is not observability-critical for gameplay. |
| GDI32.dll | CreateSolidBrush | WRAP |  | 1 | _init_directx_ovl (allegro) | Creates a GDI object handle (bitmap/DC/palette/brush). Handle identity/lifetime is exactly the kind of host-resource boundary WRAP exists for, even though the call itself is not observability-critical for gameplay. |
| GDI32.dll | DeleteDC | WRAP |  | 3 | _gfx_directx_set_mouse_sprite (allegro); _stretch_blit_from_hdc (allegro) | Destroys a GDI handle -- pair with the Create* wrap for lifecycle tracking/leak detection. |
| GDI32.dll | DeleteObject | WRAP |  | 7 | _gfx_directx_ovl_exit (allegro); _gfx_directx_set_mouse_sprite (allegro); _convert_hbitmap_to_bitmap (allegro); _stretch_blit_from_hdc (allegro) | Destroys a GDI handle -- pair with the Create* wrap for lifecycle tracking/leak detection. |
| GDI32.dll | GetDeviceCaps | DIRECT |  | 3 | _sys_directx_get_desktop_resolution (allegro); _sys_directx_desktop_color_depth (allegro) | Pure draw/query operation on an already-open DC/bitmap; no new handle is created and the operation is deterministic given deterministic pixel/palette input. Used by Allegro's GDI-mode (non-DirectDraw) software blit fallback (wgdi.c/gdi.c). |
| GDI32.dll | GetDIBits | DIRECT |  | 1 | _convert_hbitmap_to_bitmap (allegro) | Pure draw/query operation on an already-open DC/bitmap; no new handle is created and the operation is deterministic given deterministic pixel/palette input. Used by Allegro's GDI-mode (non-DirectDraw) software blit fallback (wgdi.c/gdi.c). |
| GDI32.dll | GetObjectA | DIRECT |  | 2 | _convert_hbitmap_to_bitmap (allegro) | Pure draw/query operation on an already-open DC/bitmap; no new handle is created and the operation is deterministic given deterministic pixel/palette input. Used by Allegro's GDI-mode (non-DirectDraw) software blit fallback (wgdi.c/gdi.c). |
| GDI32.dll | GetPaletteEntries | DIRECT |  | 1 | _convert_hpalette_to_palette (allegro) | Pure draw/query operation on an already-open DC/bitmap; no new handle is created and the operation is deterministic given deterministic pixel/palette input. Used by Allegro's GDI-mode (non-DirectDraw) software blit fallback (wgdi.c/gdi.c). |
| GDI32.dll | GetSystemPaletteEntries | DIRECT |  | 1 | _build_desktop_rgb_map (allegro) | Pure draw/query operation on an already-open DC/bitmap; no new handle is created and the operation is deterministic given deterministic pixel/palette input. Used by Allegro's GDI-mode (non-DirectDraw) software blit fallback (wgdi.c/gdi.c). |
| GDI32.dll | RealizePalette | DIRECT |  | 3 | _convert_hbitmap_to_bitmap (allegro); _convert_bitmap_to_hbitmap (allegro); _set_palette_to_hdc (allegro) | Pure draw/query operation on an already-open DC/bitmap; no new handle is created and the operation is deterministic given deterministic pixel/palette input. Used by Allegro's GDI-mode (non-DirectDraw) software blit fallback (wgdi.c/gdi.c). |
| GDI32.dll | SelectObject | WRAP |  | 6 | _gfx_directx_set_mouse_sprite (allegro); _stretch_blit_from_hdc (allegro) | Binds a GDI object into a DC's selection slot -- handle-identity boundary worth tracing. |
| GDI32.dll | SelectPalette | WRAP |  | 5 | _convert_hbitmap_to_bitmap (allegro); _convert_bitmap_to_hbitmap (allegro); _set_palette_to_hdc (allegro) | Binds a GDI object into a DC's selection slot -- handle-identity boundary worth tracing. |
| GDI32.dll | SetPaletteEntries | DIRECT |  | 1 | _set_palette_to_hdc (allegro) | Pure draw/query operation on an already-open DC/bitmap; no new handle is created and the operation is deterministic given deterministic pixel/palette input. Used by Allegro's GDI-mode (non-DirectDraw) software blit fallback (wgdi.c/gdi.c). |
| GDI32.dll | SetPixel | DIRECT |  | 4 | _gfx_directx_set_mouse_sprite (allegro) | Pure draw/query operation on an already-open DC/bitmap; no new handle is created and the operation is deterministic given deterministic pixel/palette input. Used by Allegro's GDI-mode (non-DirectDraw) software blit fallback (wgdi.c/gdi.c). |
| GDI32.dll | StretchBlt | DIRECT |  | 1 | _stretch_blit_from_hdc (allegro) | Pure draw/query operation on an already-open DC/bitmap; no new handle is created and the operation is deterministic given deterministic pixel/palette input. Used by Allegro's GDI-mode (non-DirectDraw) software blit fallback (wgdi.c/gdi.c). |
| GDI32.dll | StretchDIBits | DIRECT |  | 2 | _stretch_blit_to_hdc (allegro) | Pure draw/query operation on an already-open DC/bitmap; no new handle is created and the operation is deterministic given deterministic pixel/palette input. Used by Allegro's GDI-mode (non-DirectDraw) software blit fallback (wgdi.c/gdi.c). |
| KERNEL32.dll | AddAtomA | WRAP |  | 1 | ___cmshared_create_or_grab (crt) | Global atom-table entries, used by Allegro's window-class registration (wwnd.c). Host-global namespace shared across all processes -- worth tracing, low risk. |
| KERNEL32.dll | CloseHandle | WRAP |  | 10 | _mouse_dinput_exit (allegro); _key_dinput_exit (allegro); _key_directx_exit (allegro); _key_directx_init (allegro) | Generic handle-close; worth tracing since it applies to events, files, threads alike. |
| KERNEL32.dll | CreateEventA | WRAP |  | 12 | _mouse_directx_init (allegro); _key_directx_init (allegro); _tim_win32_low_perf_init (allegro); _tim_win32_high_perf_init (allegro) | Creates a named/unnamed sync-object handle (used by Allegro's timer/input threads and fld_adspot's worker thread). |
| KERNEL32.dll | DeleteCriticalSection | WRAP |  | 5 | _sys_directx_exit (allegro); _exit_directx_window (allegro); _sys_directx_destroy_mutex (allegro); _dtoa_lock_cleanup (crt) | Creates/destroys an in-process lock object; identity matters for tracking lock lifetime even though the lock itself is not a kernel handle. |
| KERNEL32.dll | DuplicateHandle | WRAP |  | 1 | _sys_directx_init (allegro) | Handle-identity duplication boundary. |
| KERNEL32.dll | EnterCriticalSection | WRAP |  | 36 | _mouse_directx_set_speed (allegro); _mouse_directx_set_range (allegro); _mouse_directx_position (allegro); _sys_directx_lock_mutex (allegro) | Lock acquire/release around Allegro's shared state (timer/input threads). Ordering matters for thread-interleaving reproducibility but this is Allegro plumbing, not gameplay logic; tracing (not full control) is enough. |
| KERNEL32.dll | ExitProcess | WRAP |  | 1 | ___mingw_CRTStartup (crt) | Process-termination boundary; the carrier needs to observe this to shut itself down cleanly. |
| KERNEL32.dll | FindAtomA | WRAP |  | 1 | ___cmshared_create_or_grab (crt) | Global atom-table entries, used by Allegro's window-class registration (wwnd.c). Host-global namespace shared across all processes -- worth tracing, low risk. |
| KERNEL32.dll | FormatMessageA | DIRECT |  | 1 | _win_err_str (allegro) | Pure string formatting of a Win32 error code; no state. |
| KERNEL32.dll | FreeLibrary | WRAP |  | 5 | _get_dx_ver (allegro) | Dynamic module load/unload boundary. A carrier needs to intercept these because the guest can load arbitrary further DLLs at runtime -- outside the static import table entirely. |
| KERNEL32.dll | GetAtomNameA | WRAP |  | 1 | _cmshared_get_ptr_from_atom (crt) | Global atom-table entries, used by Allegro's window-class registration (wwnd.c). Host-global namespace shared across all processes -- worth tracing, low risk. |
| KERNEL32.dll | GetCommandLineA | DIRECT |  | 2 | __WinMain (allegro); _main (crt) | Pure query/conversion of host state with no persistent resource created; safe to forward as-is. (GetVersion/GetVersionExA will report Win11's compatibility-shimmed version, which is a host quirk, not a carrier concern.) |
| KERNEL32.dll | GetCurrentProcess | DIRECT |  | 1 | _sys_directx_init (allegro) | Pure query/conversion of host state with no persistent resource created; safe to forward as-is. (GetVersion/GetVersionExA will report Win11's compatibility-shimmed version, which is a host quirk, not a carrier concern.) |
| KERNEL32.dll | GetCurrentThread | DIRECT |  | 1 | _sys_directx_init (allegro) | Pure query/conversion of host state with no persistent resource created; safe to forward as-is. (GetVersion/GetVersionExA will report Win11's compatibility-shimmed version, which is a host quirk, not a carrier concern.) |
| KERNEL32.dll | GetLogicalDrives | DIRECT |  | 1 | __al_drive_exists (allegro) | Pure query/conversion of host state with no persistent resource created; safe to forward as-is. (GetVersion/GetVersionExA will report Win11's compatibility-shimmed version, which is a host quirk, not a carrier concern.) |
| KERNEL32.dll | GetModuleFileNameA | DIRECT |  | 1 | _sys_directx_get_executable_name (allegro) | Pure query/conversion of host state with no persistent resource created; safe to forward as-is. (GetVersion/GetVersionExA will report Win11's compatibility-shimmed version, which is a host quirk, not a carrier concern.) |
| KERNEL32.dll | GetModuleHandleA | WRAP |  | 5 | ___gcc_register_frame (crt); _sys_directx_init (allegro); _adjust_window (allegro); __win_thread_init (allegro) | Dynamic module load/unload boundary. A carrier needs to intercept these because the guest can load arbitrary further DLLs at runtime -- outside the static import table entirely. |
| KERNEL32.dll | GetProcAddress | WRAP |  | 7 | ___gcc_register_frame (crt); _adjust_window (allegro); __win_thread_init (allegro); _get_dx_ver (allegro) | Dynamic symbol resolution -- this is the reverse of the import table: whatever the guest resolves through GetProcAddress is INVISIBLE to this static import census. Needs a runtime hook/log to discover late-bound calls (e.g. optional-API feature detection patterns common in 2011-era Windows code). |
| KERNEL32.dll | GetStartupInfoA | DIRECT |  | 1 | _main (crt) | Pure query/conversion of host state with no persistent resource created; safe to forward as-is. (GetVersion/GetVersionExA will report Win11's compatibility-shimmed version, which is a host quirk, not a carrier concern.) |
| KERNEL32.dll | GetTempPathA | DIRECT |  | 1 | _pack_fopen_chunk (allegro) | Pure query/conversion of host state with no persistent resource created; safe to forward as-is. (GetVersion/GetVersionExA will report Win11's compatibility-shimmed version, which is a host quirk, not a carrier concern.) |
| KERNEL32.dll | GetThreadPriority | DIRECT |  | 1 | __win_switch_out (allegro) | Pure query/conversion of host state with no persistent resource created; safe to forward as-is. (GetVersion/GetVersionExA will report Win11's compatibility-shimmed version, which is a host quirk, not a carrier concern.) |
| KERNEL32.dll | GetVersion | DIRECT |  | 2 | _sys_directx_init (allegro); __al_detect_filename_encoding (allegro) | Pure query/conversion of host state with no persistent resource created; safe to forward as-is. (GetVersion/GetVersionExA will report Win11's compatibility-shimmed version, which is a host quirk, not a carrier concern.) |
| KERNEL32.dll | GetVersionExA | DIRECT |  | 1 | _get_dx_ver (allegro) | Pure query/conversion of host state with no persistent resource created; safe to forward as-is. (GetVersion/GetVersionExA will report Win11's compatibility-shimmed version, which is a host quirk, not a carrier concern.) |
| KERNEL32.dll | InitializeCriticalSection | WRAP |  | 5 | _sys_directx_init (allegro); _init_directx_window (allegro); _sys_directx_create_mutex (allegro); _dtoa_lock (crt) | Creates/destroys an in-process lock object; identity matters for tracking lock lifetime even though the lock itself is not a kernel handle. |
| KERNEL32.dll | InterlockedExchange | DIRECT |  | 2 | _dtoa_lock (crt); _dtoa_lock_cleanup (crt) | Atomic op on guest-owned memory; no host-resource identity involved. |
| KERNEL32.dll | IsDBCSLeadByteEx | DIRECT |  | 1 | ___mbrtowc_cp (crt) | Pure query/conversion of host state with no persistent resource created; safe to forward as-is. (GetVersion/GetVersionExA will report Win11's compatibility-shimmed version, which is a host quirk, not a carrier concern.) |
| KERNEL32.dll | LeaveCriticalSection | WRAP |  | 43 | _mouse_directx_set_speed (allegro); _mouse_directx_set_range (allegro); _mouse_directx_position (allegro); _sys_directx_unlock_mutex (allegro) | Lock acquire/release around Allegro's shared state (timer/input threads). Ordering matters for thread-interleaving reproducibility but this is Allegro plumbing, not gameplay logic; tracing (not full control) is enough. |
| KERNEL32.dll | LoadLibraryA | WRAP |  | 5 | __mangled_main (game); _get_dx_ver (allegro) | Dynamic module load/unload boundary. A carrier needs to intercept these because the guest can load arbitrary further DLLs at runtime -- outside the static import table entirely. |
| KERNEL32.dll | MultiByteToWideChar | DIRECT |  | 4 | _key_directx_scancode_to_name (allegro); _key_dinput_handle_scancode (allegro); ___mbrtowc_cp (crt) | Pure query/conversion of host state with no persistent resource created; safe to forward as-is. (GetVersion/GetVersionExA will report Win11's compatibility-shimmed version, which is a host quirk, not a carrier concern.) |
| KERNEL32.dll | OutputDebugStringA | DIRECT |  | 4 | _sys_directx_trace_handler (allegro); _sys_directx_assert (allegro); _thread_safe_trace (allegro); _get_dx_ver (allegro) | Pure query/conversion of host state with no persistent resource created; safe to forward as-is. (GetVersion/GetVersionExA will report Win11's compatibility-shimmed version, which is a host quirk, not a carrier concern.) |
| KERNEL32.dll | PulseEvent | WRAP |  | 1 | _render_proc (allegro) | Event-object signaling; identity of the specific event handle matters for correctly coordinating Allegro's timer/input threads and fld_adspot's worker thread. |
| KERNEL32.dll | QueryPerformanceCounter | DETERMINISTIC |  | 9 | _play (game); _tim_win32_high_perf_thread (allegro) | High-resolution time source. QueryPerformanceCounter is called DIRECTLY from GAME code (_play in main.c, the core game loop) in addition to Allegro's wtimer.c -- this is the single most important determinism interception point found in this census: the game's own frame/physics timing reads QPC, not just Allegro's plumbing. |
| KERNEL32.dll | QueryPerformanceFrequency | DETERMINISTIC |  | 3 | _play (game); _tim_win32_high_perf_init (allegro) | High-resolution time source. QueryPerformanceCounter is called DIRECTLY from GAME code (_play in main.c, the core game loop) in addition to Allegro's wtimer.c -- this is the single most important determinism interception point found in this census: the game's own frame/physics timing reads QPC, not just Allegro's plumbing. |
| KERNEL32.dll | ResetEvent | WRAP |  | 1 | __win_switch_out (allegro) | Event-object signaling; identity of the specific event handle matters for correctly coordinating Allegro's timer/input threads and fld_adspot's worker thread. |
| KERNEL32.dll | SetEvent | WRAP |  | 13 | _mouse_directx_position (allegro); _key_directx_stop_wait (allegro); _key_dinput_acquire (allegro); _key_dinput_handle (allegro) | Event-object signaling; identity of the specific event handle matters for correctly coordinating Allegro's timer/input threads and fld_adspot's worker thread. |
| KERNEL32.dll | SetThreadPriority | DIRECT |  | 5 | _tim_win32_low_perf_init (allegro); _tim_win32_high_perf_init (allegro); _init_directx_window (allegro); __win_switch_out (allegro) | Pure query/conversion of host state with no persistent resource created; safe to forward as-is. (GetVersion/GetVersionExA will report Win11's compatibility-shimmed version, which is a host quirk, not a carrier concern.) |
| KERNEL32.dll | SetUnhandledExceptionFilter | WRAP | Y | 1 | ___mingw_CRTStartup (crt) | Installs a crash-handler callback. Only static caller is MinGW's own CRT startup (___mingw_CRTStartup) installing its default handler -- not game-specific, fires only on unhandled crash. |
| KERNEL32.dll | Sleep | DETERMINISTIC |  | 3 | _sys_directx_yield_timeslice (allegro); _tim_win32_rest (allegro); _dtoa_lock (crt) | Explicit time-based yield. All static call sites found are Allegro plumbing (wsystem.c yield-timeslice, wtimer.c rest) or CRT (mingwex dtoa lock backoff); no GAME-code call site was found, so this only needs control for full determinism if sub-frame thread interleaving must be pinned down, not for core gameplay logic. |
| KERNEL32.dll | VirtualProtect | WRAP |  | 2 | ___write_memory (crt) | Memory-protection manipulation/introspection. Only static callers found are MinGW's pseudo-reloc.c (___write_memory, handling runtime relocation of the .rdata->rwdata PE trick), not game anti-tamper as first suspected from beta.c's naming. Still worth tracing since a carrier lifting arbitrary code should watch page-protection changes. |
| KERNEL32.dll | VirtualQuery | WRAP |  | 1 | ___write_memory (crt) | Memory-protection manipulation/introspection. Only static callers found are MinGW's pseudo-reloc.c (___write_memory, handling runtime relocation of the .rdata->rwdata PE trick), not game anti-tamper as first suspected from beta.c's naming. Still worth tracing since a carrier lifting arbitrary code should watch page-protection changes. |
| KERNEL32.dll | WaitForMultipleObjects | DETERMINISTIC |  | 2 | _init_directx_window (allegro); _input_thread_proc (allegro) | Blocking wait on kernel sync objects, all static call sites are Allegro plumbing (wkeybd.c input wait, wtimer.c thread join/rest, wwnd.c/winput.c thread start rendezvous). Thread-scheduling primitive explicitly named in the task brief; needs control if full multi-thread determinism is required, but does not gate GAME logic directly. |
| KERNEL32.dll | WaitForSingleObject | DETERMINISTIC |  | 9 | _key_directx_wait_for_input (allegro); _tim_win32_low_perf_thread (allegro); _tim_win32_exit (allegro); _tim_win32_high_perf_thread (allegro) | Blocking wait on kernel sync objects, all static call sites are Allegro plumbing (wkeybd.c input wait, wtimer.c thread join/rest, wwnd.c/winput.c thread start rendezvous). Thread-scheduling primitive explicitly named in the task brief; needs control if full multi-thread determinism is required, but does not gate GAME logic directly. |
| KERNEL32.dll | WideCharToMultiByte | DIRECT |  | 1 | ___wcrtomb_cp (crt) | Pure query/conversion of host state with no persistent resource created; safe to forward as-is. (GetVersion/GetVersionExA will report Win11's compatibility-shimmed version, which is a host quirk, not a carrier concern.) |
| libpng3.dll | png_create_info_struct | DIRECT |  | 3 | _load_memory_png (game); _load_png_pf (game); _really_save_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_create_read_struct | DIRECT |  | 2 | _load_memory_png (game); _load_png_pf (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_create_write_struct | DIRECT |  | 1 | _really_save_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_destroy_read_struct | DIRECT |  | 6 | _load_memory_png (game); _load_png_pf (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_destroy_write_struct | DIRECT |  | 3 | _really_save_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_error | DIRECT |  | 3 | _read_data_memory (game); _read_data (game); _write_data (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_get_gAMA | DIRECT |  | 1 | _really_load_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_get_IHDR | DIRECT |  | 1 | _really_load_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_get_io_ptr | DIRECT |  | 3 | _read_data_memory (game); _read_data (game); _write_data (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_get_PLTE | DIRECT |  | 1 | _really_load_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_get_rowbytes | DIRECT |  | 1 | _really_load_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_get_sRGB | DIRECT |  | 1 | _really_load_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_get_valid | DIRECT |  | 1 | _really_load_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_read_end | DIRECT |  | 1 | _really_load_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_read_info | DIRECT |  | 1 | _really_load_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_read_row | DIRECT |  | 1 | _really_load_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_read_update_info | DIRECT |  | 1 | _really_load_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_set_bgr | DIRECT |  | 1 | _really_load_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_set_compression_level | DIRECT |  | 3 | _really_save_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_set_expand | DIRECT |  | 1 | _really_load_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_set_gamma | DIRECT |  | 2 | _really_load_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_set_gray_to_rgb | DIRECT |  | 1 | _really_load_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_set_IHDR | DIRECT |  | 3 | _really_save_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_set_interlace_handling | DIRECT |  | 1 | _really_load_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_set_packing | DIRECT |  | 1 | _really_load_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_set_PLTE | DIRECT |  | 1 | _really_save_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_set_read_fn | DIRECT | Y | 2 | _load_memory_png (game); _load_png_pf (game) | Registers a custom stream read/write function pointer that libpng calls back into during png_read_*/png_write_* -- textbook callback-bearing import (explicit example in the task brief). Callback target is GAME code (loadpng.c/savepng.c's own I/O shims). |
| libpng3.dll | png_set_sig_bytes | DIRECT |  | 2 | _load_memory_png (game); _load_png_pf (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_set_strip_16 | DIRECT |  | 1 | _really_load_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_set_tRNS_to_alpha | DIRECT |  | 1 | _really_load_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_set_write_fn | DIRECT | Y | 1 | _really_save_png (game) | Registers a custom stream read/write function pointer that libpng calls back into during png_read_*/png_write_* -- textbook callback-bearing import (explicit example in the task brief). Callback target is GAME code (loadpng.c/savepng.c's own I/O shims). |
| libpng3.dll | png_sig_cmp | DIRECT |  | 2 | _load_memory_png (game); _load_png_pf (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_write_end | DIRECT |  | 1 | _really_save_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_write_info | DIRECT |  | 3 | _really_save_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| libpng3.dll | png_write_row | DIRECT |  | 4 | _really_save_png (game) | libpng3.dll is a bundled third-party DLL shipped in assets/, not a Windows OS component -- it should simply keep being loaded/called as an ordinary DLL dependency, not 'forwarded to host Windows'. All static callers are GAME code's own PNG wrapper (loadpng.c/savepng.c/regpng.c: _really_load_png et al.). No observability/determinism concern beyond deterministic decode of deterministic file bytes. |
| msvcrt.dll | __getmainargs | DIRECT |  | 1 | ___mingw_CRTStartup (crt) | MinGW/CRT-internal startup data and initialization routines invoked once from the compiler-generated CRT glue (mingw's own main.c/crt1.c), not by GAME or Allegro code. No gameplay/determinism impact; several of these (__lc_codepage, __mb_cur_max, _iob, _pctype, _tzname) are DATA imports read directly rather than called. |
| msvcrt.dll | __lc_codepage | DIRECT |  | 5 | _wcsrtombs (crt); _wcrtomb (crt); _mbrlen (crt); _mbsrtowcs (crt) | MinGW/CRT-internal startup data and initialization routines invoked once from the compiler-generated CRT glue (mingw's own main.c/crt1.c), not by GAME or Allegro code. No gameplay/determinism impact; several of these (__lc_codepage, __mb_cur_max, _iob, _pctype, _tzname) are DATA imports read directly rather than called. |
| msvcrt.dll | __mb_cur_max | DIRECT |  | 6 | __strptime (game); _wcsrtombs (crt); _wcrtomb (crt); _mbrlen (crt) | MinGW/CRT-internal startup data and initialization routines invoked once from the compiler-generated CRT glue (mingw's own main.c/crt1.c), not by GAME or Allegro code. No gameplay/determinism impact; several of these (__lc_codepage, __mb_cur_max, _iob, _pctype, _tzname) are DATA imports read directly rather than called. |
| msvcrt.dll | __p__environ | DIRECT |  | 1 | ___mingw_CRTStartup (crt) | MinGW/CRT-internal startup data and initialization routines invoked once from the compiler-generated CRT glue (mingw's own main.c/crt1.c), not by GAME or Allegro code. No gameplay/determinism impact; several of these (__lc_codepage, __mb_cur_max, _iob, _pctype, _tzname) are DATA imports read directly rather than called. |
| msvcrt.dll | __p__fmode | DIRECT |  | 1 | ___mingw_CRTStartup (crt) | MinGW/CRT-internal startup data and initialization routines invoked once from the compiler-generated CRT glue (mingw's own main.c/crt1.c), not by GAME or Allegro code. No gameplay/determinism impact; several of these (__lc_codepage, __mb_cur_max, _iob, _pctype, _tzname) are DATA imports read directly rather than called. |
| msvcrt.dll | __set_app_type | DIRECT |  | 2 | _WinMainCRTStartup (crt); _mainCRTStartup (crt) | MinGW/CRT-internal startup data and initialization routines invoked once from the compiler-generated CRT glue (mingw's own main.c/crt1.c), not by GAME or Allegro code. No gameplay/determinism impact; several of these (__lc_codepage, __mb_cur_max, _iob, _pctype, _tzname) are DATA imports read directly rather than called. |
| msvcrt.dll | _assert | WRAP |  | 4 | ___write_memory (crt); _cmshared_get_ptr_from_atom (crt); ___cmshared_create_or_grab (crt) | Assertion-failure path (message + abort); static callers are all CRT/pseudo-reloc internals here, not GAME asserts, but worth tracing since it terminates the process. |
| msvcrt.dll | _beginthread | DETERMINISTIC | Y | 4 | _tim_win32_low_perf_init (allegro); _tim_win32_high_perf_init (allegro); _init_directx_window (allegro); __win_input_register_event (allegro) | Thread creation with a start-routine function pointer -- explicit callback-bearing example in the task brief. All 4 static callers are Allegro plumbing (wtimer.c's two timer threads, wwnd.c's directx window thread, winput.c's input-event thread) -- not called by GAME code directly (GAME's own background thread, fld_adspot's ad fetch, uses pthread_create instead, see pthreadGC2.dll). Thread creation affects scheduling/timing nondeterminism, hence DETERMINISTIC, but since every caller is Allegro-internal plumbing (timer tick, window-message pump, input polling) rather than gameplay logic, the practical fix is to make Allegro's OWN abstractions (its timer callbacks, its input polling results) deterministic/replayable, not to literally control thread creation timing itself. |
| msvcrt.dll | _cexit | WRAP |  | 1 | ___mingw_CRTStartup (crt) | Process-termination boundary, same rationale as ExitProcess -- carrier needs to observe for clean teardown. |
| msvcrt.dll | _chdir | WRAP |  | 1 | __mangled_main (game) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | _close | WRAP |  | 1 | _normal_fclose (allegro) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | _dup | WRAP |  | 3 | __pack_fdopen (allegro); _pack_fclose_chunk (allegro) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | _errno | DIRECT |  | 40 | __mangled_main (game); __get_data (vorbis/ogg); _al_trace (allegro); _al_assert (allegro) | Thread-local last-error query, pure read. |
| msvcrt.dll | _findclose | WRAP |  | 1 | _al_findclose (allegro) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | _findfirst | WRAP |  | 1 | _al_findfirst (allegro) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | _findnext | WRAP |  | 1 | _al_findnext (allegro) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | _getcwd | WRAP |  | 1 | _check_characters (game) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | _getdcwd | WRAP |  | 1 | __al_getdcwd (allegro) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | _getdrive | WRAP |  | 1 | __al_getdrive (allegro) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | _hypot | DIRECT |  | 1 | _fixhypot (allegro) | Pure deterministic math library calls, no host-resource identity. NOTE (INFERRED determinism risk, not a class change): this is a 32-bit GCC 4.4.1 build with no -mfpmath flag evidence found, i.e. it likely targets the x87 80-bit FPU by default. Forwarding these to a modern host libm (SSE2, 64-bit double throughout) can change the last bit or two of a transcendental result versus the original XP-era msvcrt.dll. Not expected to matter for this game's visible logic (no evidence any of these feed position/collision determinism the way rand()/QPC do) but worth a regression check before assuming bit-exact replay across old-binary vs carrier runs. |
| msvcrt.dll | _iob | DIRECT |  | 3 | ___mingw_CRTStartup (crt); _al_assert (allegro); _allegro_message (allegro) | MinGW/CRT-internal startup data and initialization routines invoked once from the compiler-generated CRT glue (mingw's own main.c/crt1.c), not by GAME or Allegro code. No gameplay/determinism impact; several of these (__lc_codepage, __mb_cur_max, _iob, _pctype, _tzname) are DATA imports read directly rather than called. |
| msvcrt.dll | _isctype | DIRECT |  | 3 | __strptime (game) | Locale/ctype queries; static, no host-identity or determinism concern for gameplay. |
| msvcrt.dll | _lseek | WRAP |  | 9 | _normal_fseek (allegro); _normal_flush_buffer (allegro); __pack_fdopen (allegro); _pack_fclose_chunk (allegro) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | _mkdir | WRAP |  | 5 | _fldads_get_local_cache_name (game); _init_game (game); _play (game); _save_profile (game) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | _onexit | DIRECT |  | 1 | __onexit (crt) | MinGW/CRT-internal startup data and initialization routines invoked once from the compiler-generated CRT glue (mingw's own main.c/crt1.c), not by GAME or Allegro code. No gameplay/determinism impact; several of these (__lc_codepage, __mb_cur_max, _iob, _pctype, _tzname) are DATA imports read directly rather than called. |
| msvcrt.dll | _open | WRAP |  | 2 | _pack_fopen_chunk (allegro); __al_win_open (allegro) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | _pctype | DIRECT |  | 2 | __strptime (game) | MinGW/CRT-internal startup data and initialization routines invoked once from the compiler-generated CRT glue (mingw's own main.c/crt1.c), not by GAME or Allegro code. No gameplay/determinism impact; several of these (__lc_codepage, __mb_cur_max, _iob, _pctype, _tzname) are DATA imports read directly rather than called. |
| msvcrt.dll | _read | WRAP |  | 2 | _normal_getc (allegro) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | _rmdir | WRAP |  | 2 | _delete_profile (game) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | _setjmp | DIRECT |  | 3 | _load_memory_png (game); _load_png_pf (game); _really_save_png (game) | Non-local jump / saved CPU register state; no host-resource identity, deterministic. |
| msvcrt.dll | _setmode | WRAP |  | 3 | ___mingw_CRTStartup (crt) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | _stat | WRAP |  | 3 | _fldads_load_cache_from_csv (game); _fldads_update_local_adimg (game); _fldads_threadmain (game) | KNOWN, two separate IAT slots share this name: one used by GAME's fld_adspot.c (_fldads_load_cache_from_csv/_update_local_adimg/_threadmain -- checking cached ad-image file timestamps/existence) and one used by Allegro's wfile.c (__al_file_time/__al_file_size_ex -- generic file-info queries). Same WRAP rationale as the rest of the file-I/O family; listed once here, both slots appear in the machine-readable JSON. |
| msvcrt.dll | _stat | WRAP |  | 2 | __al_file_time (allegro); __al_file_size_ex (allegro) | KNOWN, two separate IAT slots share this name: one used by GAME's fld_adspot.c (_fldads_load_cache_from_csv/_update_local_adimg/_threadmain -- checking cached ad-image file timestamps/existence) and one used by Allegro's wfile.c (__al_file_time/__al_file_size_ex -- generic file-info queries). Same WRAP rationale as the rest of the file-I/O family; listed once here, both slots appear in the machine-readable JSON. |
| msvcrt.dll | _strdup | DIRECT |  | 24 | _fldads_load_cache_from_csv (game); _SplitURL (game); _init_game (game); _logg_get_stream (allegro) | Pure string/buffer operations, deterministic given deterministic inputs; printf writes to the process's (normally invisible, GUI-subsystem) stdout stream. |
| msvcrt.dll | _stricmp | DIRECT |  | 33 | _loadCustomSoundFILE (game); _load_sounds (game); _set_current_avatar (game); _draw_results (game) | Pure string/buffer operations, deterministic given deterministic inputs; printf writes to the process's (normally invisible, GUI-subsystem) stdout stream. |
| msvcrt.dll | _strnicmp | DIRECT |  | 1 | _match_string (game) | Pure string/buffer operations, deterministic given deterministic inputs; printf writes to the process's (normally invisible, GUI-subsystem) stdout stream. |
| msvcrt.dll | _tzname | WRAP |  | 1 | __strptime (game) | Host timezone configuration query -- affects localtime() results, environment-dependent. |
| msvcrt.dll | _tzset | WRAP |  | 1 | __strptime (game) | Host timezone configuration query -- affects localtime() results, environment-dependent. |
| msvcrt.dll | _unlink | WRAP |  | 1 | __al_win_unlink (allegro) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | _wfindfirst | WRAP |  | 1 | _al_findfirst (allegro) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | _wfindnext | WRAP |  | 1 | _al_findnext (allegro) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | _wgetdcwd | WRAP |  | 1 | __al_getdcwd (allegro) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | _wopen | WRAP |  | 1 | __al_win_open (allegro) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | _write | WRAP |  | 2 | _normal_flush_buffer (allegro) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | _wstat | WRAP |  | 2 | __al_file_time (allegro); __al_file_size_ex (allegro) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | _wunlink | WRAP |  | 1 | __al_win_unlink (allegro) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | abort | WRAP |  | 3 | _al_assert (allegro); _sys_directx_assert (allegro); _key_dinput_handle_scancode (allegro) | Process-termination boundary, same rationale as ExitProcess -- carrier needs to observe for clean teardown. |
| msvcrt.dll | acos | DIRECT |  | 2 | _vorbis_lpc_to_lsp (vorbis/ogg) | Pure deterministic math library calls, no host-resource identity. NOTE (INFERRED determinism risk, not a class change): this is a 32-bit GCC 4.4.1 build with no -mfpmath flag evidence found, i.e. it likely targets the x87 80-bit FPU by default. Forwarding these to a modern host libm (SSE2, 64-bit double throughout) can change the last bit or two of a transcendental result versus the original XP-era msvcrt.dll. Not expected to matter for this game's visible logic (no evidence any of these feed position/collision determinism the way rand()/QPC do) but worth a regression check before assuming bit-exact replay across old-binary vs carrier runs. |
| msvcrt.dll | atan | DIRECT |  | 12 | __analysis_output_always (vorbis/ogg); __vp_psy_init (vorbis/ogg); _floor0_inverse2 (vorbis/ogg) | Pure deterministic math library calls, no host-resource identity. NOTE (INFERRED determinism risk, not a class change): this is a 32-bit GCC 4.4.1 build with no -mfpmath flag evidence found, i.e. it likely targets the x87 80-bit FPU by default. Forwarding these to a modern host libm (SSE2, 64-bit double throughout) can change the last bit or two of a transcendental result versus the original XP-era msvcrt.dll. Not expected to matter for this game's visible logic (no evidence any of these feed position/collision determinism the way rand()/QPC do) but worth a regression check before assuming bit-exact replay across old-binary vs carrier runs. |
| msvcrt.dll | atexit | WRAP | Y | 3 | ___do_global_ctors (crt); _dtoa_lock (crt); _register_frame_ctor (crt) | Registers an exit-time cleanup callback -- callback-bearing. Reached in this binary only through the CRT's own internal _atexit/__onexit trampoline wrappers (crt1.c), which forward to these msvcrt imports via a load-then-indirect-jump thunk rather than a direct call/jmp *IAT -- a second import-thunk idiom distinct from the usual single-instruction 'jmp *0x51xxxx' pattern, found while parsing the disassembly. |
| msvcrt.dll | atof | DIRECT |  | 2 | _really_load_png (game); _uatof (allegro) | Pure string-to-number parsing, deterministic given deterministic input text. |
| msvcrt.dll | calloc | DIRECT |  | 41 | _logg_get_stream (allegro); __ov_open1 (vorbis/ogg); _vorbis_info_init (vorbis/ogg); _vorbis_synthesis_headerin (vorbis/ogg) | Pure heap/memory operations on guest-owned memory; no host-resource identity involved, used everywhere (GAME, Allegro, libvorbis/libogg). |
| msvcrt.dll | ceil | DIRECT |  | 1 | _setup_tone_curves (vorbis/ogg) | Pure deterministic math library calls, no host-resource identity. NOTE (INFERRED determinism risk, not a class change): this is a 32-bit GCC 4.4.1 build with no -mfpmath flag evidence found, i.e. it likely targets the x87 80-bit FPU by default. Forwarding these to a modern host libm (SSE2, 64-bit double throughout) can change the last bit or two of a transcendental result versus the original XP-era msvcrt.dll. Not expected to matter for this game's visible logic (no evidence any of these feed position/collision determinism the way rand()/QPC do) but worth a regression check before assuming bit-exact replay across old-binary vs carrier runs. |
| msvcrt.dll | clock | DETERMINISTIC |  | 8 | _play (game); _rest_callback (allegro) | Wall-clock/CPU-clock time sources -- explicit 'time' category. Used for timestamping (hi-score dates, profile/save data, replay files, log entries) and possibly session pacing; must be virtualized for reproducible replay/testing. |
| msvcrt.dll | cos | DIRECT |  | 9 | _mdct_init (vorbis/ogg); _dradbg (vorbis/ogg); _dradfg (vorbis/ogg); _drft_init (vorbis/ogg) | Pure deterministic math library calls, no host-resource identity. NOTE (INFERRED determinism risk, not a class change): this is a 32-bit GCC 4.4.1 build with no -mfpmath flag evidence found, i.e. it likely targets the x87 80-bit FPU by default. Forwarding these to a modern host libm (SSE2, 64-bit double throughout) can change the last bit or two of a transcendental result versus the original XP-era msvcrt.dll. Not expected to matter for this game's visible logic (no evidence any of these feed position/collision determinism the way rand()/QPC do) but worth a regression check before assuming bit-exact replay across old-binary vs carrier runs. |
| msvcrt.dll | exit | WRAP |  | 2 | __mangled_main (game); _floor1_fit (vorbis/ogg) | Process-termination boundary, same rationale as ExitProcess -- carrier needs to observe for clean teardown. |
| msvcrt.dll | exp | DIRECT |  | 7 | _setup_tone_curves (vorbis/ogg); __vp_psy_init (vorbis/ogg) | Pure deterministic math library calls, no host-resource identity. NOTE (INFERRED determinism risk, not a class change): this is a 32-bit GCC 4.4.1 build with no -mfpmath flag evidence found, i.e. it likely targets the x87 80-bit FPU by default. Forwarding these to a modern host libm (SSE2, 64-bit double throughout) can change the last bit or two of a transcendental result versus the original XP-era msvcrt.dll. Not expected to matter for this game's visible logic (no evidence any of these feed position/collision determinism the way rand()/QPC do) but worth a regression check before assuming bit-exact replay across old-binary vs carrier runs. |
| msvcrt.dll | fclose | WRAP |  | 32 | _save_garbled_data (game); _load_garbled_data (game); _load_plain_data (game); _csv_open (game) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | fflush | WRAP |  | 2 | _al_trace (allegro); _al_assert (allegro) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | fgets | WRAP |  | 10 | _load_sounds (game); _load_frames (game); _load_character_bmp (game) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | floor | DIRECT |  | 26 | _ov_bitrate (vorbis/ogg); __book_maptype1_quantvals (vorbis/ogg); __float32_pack (vorbis/ogg); __book_unquantize (vorbis/ogg) | Pure deterministic math library calls, no host-resource identity. NOTE (INFERRED determinism risk, not a class change): this is a 32-bit GCC 4.4.1 build with no -mfpmath flag evidence found, i.e. it likely targets the x87 80-bit FPU by default. Forwarding these to a modern host libm (SSE2, 64-bit double throughout) can change the last bit or two of a transcendental result versus the original XP-era msvcrt.dll. Not expected to matter for this game's visible logic (no evidence any of these feed position/collision determinism the way rand()/QPC do) but worth a regression check before assuming bit-exact replay across old-binary vs carrier runs. |
| msvcrt.dll | fmod | DIRECT |  | 1 | _hsv_to_rgb (allegro) | Pure deterministic math library calls, no host-resource identity. NOTE (INFERRED determinism risk, not a class change): this is a 32-bit GCC 4.4.1 build with no -mfpmath flag evidence found, i.e. it likely targets the x87 80-bit FPU by default. Forwarding these to a modern host libm (SSE2, 64-bit double throughout) can change the last bit or two of a transcendental result versus the original XP-era msvcrt.dll. Not expected to matter for this game's visible logic (no evidence any of these feed position/collision determinism the way rand()/QPC do) but worth a regression check before assuming bit-exact replay across old-binary vs carrier runs. |
| msvcrt.dll | fopen | WRAP |  | 26 | _save_garbled_data (game); _load_garbled_data (game); _load_plain_data (game); _csv_open (game) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | fprintf | WRAP |  | 13 | _fldads_dump_local_cache (game); _dumpHTTPResponse (game); __mangled_main (game); _save_profile (game) | Formatted write through a FILE* handle (frequently the log/ad-cache/response-dump files in httpget.c/fld_adspot.c/profile.c, or __iob's stdout/stderr) -- file-handle boundary, grouped with the other file I/O above. |
| msvcrt.dll | fputc | WRAP |  | 3 | _log2file (game); _save_profile (game); ___pformat_putc (crt) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | fputs | WRAP |  | 5 | _save_profile (game); _allegro_message (allegro) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | fread | WRAP |  | 10 | _read_line (game); _load_garbled_data (game); _load_control (game); _csv_open (game) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | free | DIRECT |  | 190 | _destroy_all (game); _csv_destroy (game); _fldads_destroy_cache (game); _destroy_game_data (game) | Pure heap/memory operations on guest-owned memory; no host-resource identity involved, used everywhere (GAME, Allegro, libvorbis/libogg). |
| msvcrt.dll | frexp | DIRECT |  | 1 | _vorbis_lsp_to_curve (vorbis/ogg) | Pure deterministic math library calls, no host-resource identity. NOTE (INFERRED determinism risk, not a class change): this is a 32-bit GCC 4.4.1 build with no -mfpmath flag evidence found, i.e. it likely targets the x87 80-bit FPU by default. Forwarding these to a modern host libm (SSE2, 64-bit double throughout) can change the last bit or two of a transcendental result versus the original XP-era msvcrt.dll. Not expected to matter for this game's visible logic (no evidence any of these feed position/collision determinism the way rand()/QPC do) but worth a regression check before assuming bit-exact replay across old-binary vs carrier runs. |
| msvcrt.dll | fseek | WRAP |  | 2 | __ov_header_fseek_wrap (allegro); __fseek64_wrap (vorbis/ogg) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | ftell | WRAP |  | 0 | _(no static call site found)_ | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | fwrite | WRAP |  | 11 | _save_garbled_data (game); _save_control (game); _fldads_update_local_adimg (game); _loadScrambled (game) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | getenv | WRAP |  | 6 | _really_load_png (game); _al_trace (allegro); _al_assert (allegro); _find_allegro_resource (allegro) | Reads a HOST environment variable. Static callers include GAME's loadpng.c (_really_load_png -- likely a debug/override env var) and Allegro's allegro.c/file.c (resource path discovery). Environment differs between the original XP-era machine and any carrier host, so this needs virtualizing for reproducible directory/config resolution. |
| msvcrt.dll | gmtime | DETERMINISTIC |  | 1 | _timegm (game) | Wall-clock/CPU-clock time sources -- explicit 'time' category. Used for timestamping (hi-score dates, profile/save data, replay files, log entries) and possibly session pacing; must be virtualized for reproducible replay/testing. |
| msvcrt.dll | ldexp | DIRECT |  | 4 | __float32_unpack (vorbis/ogg); __float32_pack (vorbis/ogg); __book_unquantize (vorbis/ogg) | Pure deterministic math library calls, no host-resource identity. NOTE (INFERRED determinism risk, not a class change): this is a 32-bit GCC 4.4.1 build with no -mfpmath flag evidence found, i.e. it likely targets the x87 80-bit FPU by default. Forwarding these to a modern host libm (SSE2, 64-bit double throughout) can change the last bit or two of a transcendental result versus the original XP-era msvcrt.dll. Not expected to matter for this game's visible logic (no evidence any of these feed position/collision determinism the way rand()/QPC do) but worth a regression check before assuming bit-exact replay across old-binary vs carrier runs. |
| msvcrt.dll | localeconv | DIRECT |  | 3 | ___strtodg (crt); ___pformat_emit_radix_point (crt); ___gethex_D2A (crt) | Locale/ctype queries; static, no host-identity or determinism concern for gameplay. |
| msvcrt.dll | localtime | DETERMINISTIC |  | 4 | _save_profile (game); _create_profile (game); _save_replay (game); _timegm (game) | Wall-clock/CPU-clock time sources -- explicit 'time' category. Used for timestamping (hi-score dates, profile/save data, replay files, log entries) and possibly session pacing; must be virtualized for reproducible replay/testing. |
| msvcrt.dll | log | DIRECT |  | 12 | __float32_pack (vorbis/ogg); _setup_tone_curves (vorbis/ogg); __vp_psy_init (vorbis/ogg); _mdct_init (vorbis/ogg) | Pure deterministic math library calls, no host-resource identity. NOTE (INFERRED determinism risk, not a class change): this is a 32-bit GCC 4.4.1 build with no -mfpmath flag evidence found, i.e. it likely targets the x87 80-bit FPU by default. Forwarding these to a modern host libm (SSE2, 64-bit double throughout) can change the last bit or two of a transcendental result versus the original XP-era msvcrt.dll. Not expected to matter for this game's visible logic (no evidence any of these feed position/collision determinism the way rand()/QPC do) but worth a regression check before assuming bit-exact replay across old-binary vs carrier runs. |
| msvcrt.dll | log10 | DIRECT |  | 7 | _digi_directsound_init (allegro); _digi_dsoundmix_init (allegro) | Pure deterministic math library calls, no host-resource identity. NOTE (INFERRED determinism risk, not a class change): this is a 32-bit GCC 4.4.1 build with no -mfpmath flag evidence found, i.e. it likely targets the x87 80-bit FPU by default. Forwarding these to a modern host libm (SSE2, 64-bit double throughout) can change the last bit or two of a transcendental result versus the original XP-era msvcrt.dll. Not expected to matter for this game's visible logic (no evidence any of these feed position/collision determinism the way rand()/QPC do) but worth a regression check before assuming bit-exact replay across old-binary vs carrier runs. |
| msvcrt.dll | malloc | DIRECT |  | 89 | _create_post (game); _csv_begin (game); _csv_open (game); _create_game_data (game) | Pure heap/memory operations on guest-owned memory; no host-resource identity involved, used everywhere (GAME, Allegro, libvorbis/libogg). |
| msvcrt.dll | memchr | DIRECT |  | 1 | _ogg_sync_pageseek (vorbis/ogg) | Pure heap/memory operations on guest-owned memory; no host-resource identity involved, used everywhere (GAME, Allegro, libvorbis/libogg). |
| msvcrt.dll | memcpy | DIRECT |  | 21 | __ov_open1 (vorbis/ogg); __ov_getlap (vorbis/ogg); _vorbis_analysis_headerout (vorbis/ogg); _vorbis_commentheader_out (vorbis/ogg) | Pure heap/memory operations on guest-owned memory; no host-resource identity involved, used everywhere (GAME, Allegro, libvorbis/libogg). |
| msvcrt.dll | memmove | DIRECT |  | 24 | _vorbis_analysis_blockout (vorbis/ogg); __ve_envelope_shift (vorbis/ogg); _ogg_stream_flush (vorbis/ogg); _ogg_stream_pagein (vorbis/ogg) | Pure heap/memory operations on guest-owned memory; no host-resource identity involved, used everywhere (GAME, Allegro, libvorbis/libogg). |
| msvcrt.dll | memset | DIRECT |  | 35 | _ov_clear (vorbis/ogg); __ov_open1 (vorbis/ogg); __ov_getlap (vorbis/ogg); __vds_shared_init (vorbis/ogg) | Pure heap/memory operations on guest-owned memory; no host-resource identity involved, used everywhere (GAME, Allegro, libvorbis/libogg). |
| msvcrt.dll | mktime | DETERMINISTIC |  | 3 | _timegm (game) | Wall-clock/CPU-clock time sources -- explicit 'time' category. Used for timestamping (hi-score dates, profile/save data, replay files, log entries) and possibly session pacing; must be virtualized for reproducible replay/testing. |
| msvcrt.dll | perror | WRAP |  | 1 | __analysis_output_always (vorbis/ogg) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | pow | DIRECT |  | 11 | __book_maptype1_quantvals (vorbis/ogg); __book_unquantize (vorbis/ogg); _res0_look (vorbis/ogg); _digi_directsound_get_mixer_volume (allegro) | Pure deterministic math library calls, no host-resource identity. NOTE (INFERRED determinism risk, not a class change): this is a 32-bit GCC 4.4.1 build with no -mfpmath flag evidence found, i.e. it likely targets the x87 80-bit FPU by default. Forwarding these to a modern host libm (SSE2, 64-bit double throughout) can change the last bit or two of a transcendental result versus the original XP-era msvcrt.dll. Not expected to matter for this game's visible logic (no evidence any of these feed position/collision determinism the way rand()/QPC do) but worth a regression check before assuming bit-exact replay across old-binary vs carrier runs. |
| msvcrt.dll | printf | DIRECT |  | 3 | _init_game (game); _play (game); __mangled_main (game) | Pure string/buffer operations, deterministic given deterministic inputs; printf writes to the process's (normally invisible, GUI-subsystem) stdout stream. |
| msvcrt.dll | qsort | DIRECT | Y | 17 | _update_file_list (game); _vorbis_book_init_decode (vorbis/ogg); __vp_noise_normalize_sort (vorbis/ogg); __vp_quantize_couple_sort (vorbis/ogg) | Callback-bearing (comparator function pointer -- explicit example in task brief). GAME's only use (_update_file_list, replay.c) sorts the replay-browser file list for UI display, not gameplay/replay-affecting data; other static callers are Allegro (gui.c focus order, graphics.c mode list, quantize.c palette generation) and libvorbis/libogg internals (codebook/psy/floor setup) -- none of these affect game determinism. INFERRED caveat: msvcrt's qsort is not guaranteed stable, so a different host CRT's qsort could reorder equal-key elements differently; irrelevant here since no observed use sorts anything gameplay- or score-critical, but flagged in case that assumption is wrong for an unobserved dynamic code path. |
| msvcrt.dll | rand | DETERMINISTIC |  | 18 | _create_post (game); _fldads_get_random_ad (game); _new_game (game); _init_game (game) | KNOWN, HIGH PRIORITY: called directly by GAME code -- _create_post (beta.c, hidden/obfuscated module), _fldads_get_random_ad (fld_adspot.c, ad rotation -- not gameplay), and _new_game (main.c). The _new_game call site is the concerning one: this is the core per-run gameplay RNG (likely floor/obstacle layout, item spawns). Because rand() here is msvcrt's own PRNG algorithm (LCG-based, NOT reimplemented by the game), a carrier that forwards this to a *different* CRT/host rand() implementation, or a host whose msvcrt.dll rand() was patched/changed across Windows versions, would silently desync all replays. This must be intercepted with a game-controlled deterministic PRNG seeded/recorded explicitly, not naively forwarded. |
| msvcrt.dll | realloc | DIRECT |  | 25 | _csv_add_field (game); _csv_open (game); _fldads_load_cache_from_csv (game); _extractHTTPResponse (game) | Pure heap/memory operations on guest-owned memory; no host-resource identity involved, used everywhere (GAME, Allegro, libvorbis/libogg). |
| msvcrt.dll | signal | WRAP | Y | 6 | __gnu_exception_handler@4 (crt) | Installs a signal handler callback (SIGABRT etc.) -- callback-bearing, standard CRT plumbing. |
| msvcrt.dll | sin | DIRECT |  | 11 | _mdct_init (vorbis/ogg); _dradbg (vorbis/ogg); _dradfg (vorbis/ogg); _drft_init (vorbis/ogg) | Pure deterministic math library calls, no host-resource identity. NOTE (INFERRED determinism risk, not a class change): this is a 32-bit GCC 4.4.1 build with no -mfpmath flag evidence found, i.e. it likely targets the x87 80-bit FPU by default. Forwarding these to a modern host libm (SSE2, 64-bit double throughout) can change the last bit or two of a transcendental result versus the original XP-era msvcrt.dll. Not expected to matter for this game's visible logic (no evidence any of these feed position/collision determinism the way rand()/QPC do) but worth a regression check before assuming bit-exact replay across old-binary vs carrier runs. |
| msvcrt.dll | sprintf | DIRECT |  | 117 | _load_frames (game); _load_character_bmp (game); _get_profile_dir_for_profile (game); _getGameDataXML (game) | Pure string/buffer operations, deterministic given deterministic inputs; printf writes to the process's (normally invisible, GUI-subsystem) stdout stream. |
| msvcrt.dll | sqrt | DIRECT |  | 9 | __vp_quantize_couple_memo (vorbis/ogg); _Laguerre_With_Deflation (vorbis/ogg); _fixsqrt (allegro); __soft_spline (allegro) | Pure deterministic math library calls, no host-resource identity. NOTE (INFERRED determinism risk, not a class change): this is a 32-bit GCC 4.4.1 build with no -mfpmath flag evidence found, i.e. it likely targets the x87 80-bit FPU by default. Forwarding these to a modern host libm (SSE2, 64-bit double throughout) can change the last bit or two of a transcendental result versus the original XP-era msvcrt.dll. Not expected to matter for this game's visible logic (no evidence any of these feed position/collision determinism the way rand()/QPC do) but worth a regression check before assuming bit-exact replay across old-binary vs carrier runs. |
| msvcrt.dll | srand | DETERMINISTIC |  | 3 | _new_game (game); _init_game (game) | KNOWN: called directly by GAME code (_new_game x2, _init_game -- main.c), i.e. the game seeds its own gameplay RNG itself (likely from time() or similar at run start). This is exactly the seed-capture point a replay system must record. |
| msvcrt.dll | sscanf | DIRECT |  | 1 | _extractHTTPResponse (game) | Pure string/buffer operations, deterministic given deterministic inputs; printf writes to the process's (normally invisible, GUI-subsystem) stdout stream. |
| msvcrt.dll | strcat | DIRECT |  | 25 | _load_sounds (game); _load_frames (game); _load_character_bmp (game); _fldads_get_local_cache_name (game) | Pure string/buffer operations, deterministic given deterministic inputs; printf writes to the process's (normally invisible, GUI-subsystem) stdout stream. |
| msvcrt.dll | strchr | DIRECT |  | 2 | _replaceBadCharacters (game); _get_string (game) | Pure string/buffer operations, deterministic given deterministic inputs; printf writes to the process's (normally invisible, GUI-subsystem) stdout stream. |
| msvcrt.dll | strcmp | DIRECT |  | 2 | __strptime (game) | Pure string/buffer operations, deterministic given deterministic inputs; printf writes to the process's (normally invisible, GUI-subsystem) stdout stream. |
| msvcrt.dll | strcpy | DIRECT |  | 45 | _init_custom (game); _load_sounds (game); _load_frames (game); _load_character_bmp (game) | Pure string/buffer operations, deterministic given deterministic inputs; printf writes to the process's (normally invisible, GUI-subsystem) stdout stream. |
| msvcrt.dll | strerror | DIRECT |  | 1 | _ustrerror (allegro) | Pure errno-to-string table lookup, no host state. |
| msvcrt.dll | strlen | DIRECT |  | 13 | _vorbis_comment_query (vorbis/ogg); _vorbis_comment_add (vorbis/ogg); _vorbis_comment_add_tag (vorbis/ogg); __vorbis_pack_comment (vorbis/ogg) | Pure string/buffer operations, deterministic given deterministic inputs; printf writes to the process's (normally invisible, GUI-subsystem) stdout stream. |
| msvcrt.dll | strncat | DIRECT |  | 1 | __al_sane_strncpy (allegro) | Pure string/buffer operations, deterministic given deterministic inputs; printf writes to the process's (normally invisible, GUI-subsystem) stdout stream. |
| msvcrt.dll | strncmp | DIRECT |  | 2 | _get_string_data (game); _check_beta_tester (game) | Pure string/buffer operations, deterministic given deterministic inputs; printf writes to the process's (normally invisible, GUI-subsystem) stdout stream. |
| msvcrt.dll | strncpy | DIRECT |  | 10 | _get_adcache_dir (game); _get_configfile_path (game); _get_logfile_path (game); _get_profiles_dir (game) | Pure string/buffer operations, deterministic given deterministic inputs; printf writes to the process's (normally invisible, GUI-subsystem) stdout stream. |
| msvcrt.dll | strpbrk | DIRECT |  | 1 | _pack_fopen (allegro) | Pure string/buffer operations, deterministic given deterministic inputs; printf writes to the process's (normally invisible, GUI-subsystem) stdout stream. |
| msvcrt.dll | strtol | DIRECT |  | 17 | _SplitURL (game); __strptime (game); _ustrtol (allegro) | Pure string-to-number parsing, deterministic given deterministic input text. |
| msvcrt.dll | time | DETERMINISTIC |  | 22 | _fldads_threadmain (game); _new_game (game); _init_game (game); _play (game) | Wall-clock/CPU-clock time sources -- explicit 'time' category. Used for timestamping (hi-score dates, profile/save data, replay files, log entries) and possibly session pacing; must be virtualized for reproducible replay/testing. |
| msvcrt.dll | tmpnam | WRAP |  | 1 | _pack_fopen_chunk (allegro) | File-handle / filesystem-path boundary ('files, handles' -- explicit WRAP examples). Used throughout GAME code for config/profile/hiscore/replay/custom-character/ad-cache persistence (custom.c, profile.c, hisc.c, replay.c, fld_adspot.c) as well as Allegro's file.c/wfile.c and CRT startup. A carrier needs to virtualize/observe this boundary regardless of determinism, simply to relocate the game's data directory correctly. |
| msvcrt.dll | toupper | DIRECT |  | 4 | _vorbis_comment_query (vorbis/ogg); _vorbis_comment_query_count (vorbis/ogg) | Locale/ctype queries; static, no host-identity or determinism concern for gameplay. |
| msvcrt.dll | vfprintf | WRAP |  | 1 | _log2file (game) | Formatted write through a FILE* handle (frequently the log/ad-cache/response-dump files in httpget.c/fld_adspot.c/profile.c, or __iob's stdout/stderr) -- file-handle boundary, grouped with the other file I/O above. |
| msvcrt.dll | vsprintf | DIRECT |  | 3 | _log2file (game); _al_trace (allegro); _thread_safe_trace (allegro) | Pure string/buffer operations, deterministic given deterministic inputs; printf writes to the process's (normally invisible, GUI-subsystem) stdout stream. |
| msvcrt.dll | wcslen | DIRECT |  | 1 | ___mingw_pformat (crt) | Pure string/buffer operations, deterministic given deterministic inputs; printf writes to the process's (normally invisible, GUI-subsystem) stdout stream. |
| OLE32.dll | CoCreateInstance | WRAP | Y | 2 | _digi_directsound_capture_detect (allegro); _digi_directsound_capture_init (allegro) | COM activation -- only static callers are wdsinput.c's DirectSound-capture detect/init (_digi_directsound_capture_detect/init), almost certainly instantiating a DirectSoundCapture-family COM object for microphone/line-in support. Vtable calls on the returned object are invisible to this import census (see COM boundary section). |
| OLE32.dll | CoInitialize | WRAP |  | 1 | __win_thread_init (allegro) | COM apartment init, called once per new thread from Allegro's wthread.c (__win_thread_init). |
| OLE32.dll | CoUninitialize | WRAP |  | 1 | __win_thread_exit (allegro) | COM apartment teardown, pairs with CoInitialize. |
| pthreadGC2.dll | pthread_create | DETERMINISTIC | Y | 1 | _fldads_start (game) | Thread creation with a start-routine function pointer -- callback-bearing, and thread creation affects scheduling/timing nondeterminism. Sole static caller is GAME's _fldads_start (fld_adspot.c) spinning up the ad-fetch background thread -- again the ad subsystem, not core gameplay, so its nondeterminism is containable by sandboxing that one thread rather than needing full scheduler control. |
| pthreadGC2.dll | pthread_mutex_lock | WRAP |  | 4 | _fldads_get_random_ad (game); _fldads_dump_local_cache (game); _fldads_load_cache_from_csv (game); _log2file (game) | Mutex used to guard the ad-cache data shared between _fldads_start's worker thread and the main thread; lock-identity/ordering boundary, ancillary to gameplay. |
| pthreadGC2.dll | pthread_mutex_unlock | WRAP |  | 4 | _fldads_get_random_ad (game); _fldads_dump_local_cache (game); _fldads_load_cache_from_csv (game); _log2file (game) | Mutex used to guard the ad-cache data shared between _fldads_start's worker thread and the main thread; lock-identity/ordering boundary, ancillary to gameplay. |
| SHELL32.DLL | ShellExecuteA | WRAP |  | 1 | _open_web_browser (game) | Launches the OS shell / default browser (_open_web_browser in main.c, GAME code -- almost certainly the menu's 'visit website' link). Security/observability boundary: a carrier should intercept this rather than let a lifted 2012 binary silently open a URL through the modern shell. |
| USER32.dll | AdjustWindowRect | WRAP |  | 1 | _adjust_window (allegro) | Window handle creation/management -- 'windows' are explicitly named in the WRAP definition. All static callers are Allegro's wwnd.c/wddraw*.c/wsystem.c window driver. |
| USER32.dll | BeginPaint | WRAP |  | 1 | _directx_wnd_proc@16 (allegro) | DC/paint-cycle handle boundary tied to the window's GDI surface. |
| USER32.dll | CallWindowProcA | WRAP | Y | 1 | _directx_wnd_proc@16 (allegro) | Explicitly re-invokes a WNDPROC (guest code) by pointer -- callback re-entry point. |
| USER32.dll | ClientToScreen | WRAP |  | 6 | _init_directx_window (allegro); _update_matching_window (allegro); _update_colorconv_window (allegro) | Window handle creation/management -- 'windows' are explicitly named in the WRAP definition. All static callers are Allegro's wwnd.c/wddraw*.c/wsystem.c window driver. |
| USER32.dll | CreateIconIndirect | WRAP |  | 1 | _gfx_directx_set_mouse_sprite (allegro) | GDI cursor/icon resource handles -- window-chrome, not gameplay-relevant, but a resource-identity boundary per the WRAP definition. |
| USER32.dll | CreateWindowExA | WRAP |  | 1 | _create_directx_window (allegro) | Window handle creation/management -- 'windows' are explicitly named in the WRAP definition. All static callers are Allegro's wwnd.c/wddraw*.c/wsystem.c window driver. |
| USER32.dll | DefWindowProcA | WRAP |  | 1 | _directx_wnd_proc@16 (allegro) | Window handle creation/management -- 'windows' are explicitly named in the WRAP definition. All static callers are Allegro's wwnd.c/wddraw*.c/wsystem.c window driver. |
| USER32.dll | DestroyIcon | WRAP |  | 2 | _gfx_directx_hide_mouse (allegro); _gfx_directx_set_mouse_sprite (allegro) | GDI cursor/icon resource handles -- window-chrome, not gameplay-relevant, but a resource-identity boundary per the WRAP definition. |
| USER32.dll | DestroyWindow | WRAP |  | 1 | _directx_wnd_proc@16 (allegro) | Window handle creation/management -- 'windows' are explicitly named in the WRAP definition. All static callers are Allegro's wwnd.c/wddraw*.c/wsystem.c window driver. |
| USER32.dll | DispatchMessageA | WRAP | Y | 1 | _wnd_thread_proc (allegro) | The Windows message pump's dispatch step -- functionally re-enters the guest WndProc registered via RegisterClassA. Not a parameter-passed callback, but the same guest-reentrancy boundary in effect; flagged for completeness. |
| USER32.dll | EnableMenuItem | WRAP |  | 2 | _sys_directx_set_close_button_callback (allegro) | Window handle creation/management -- 'windows' are explicitly named in the WRAP definition. All static callers are Allegro's wwnd.c/wddraw*.c/wsystem.c window driver. |
| USER32.dll | EndPaint | WRAP |  | 1 | _directx_wnd_proc@16 (allegro) | DC/paint-cycle handle boundary tied to the window's GDI surface. |
| USER32.dll | GetActiveWindow | WRAP |  | 1 | _directx_wnd_proc@16 (allegro) | Window handle creation/management -- 'windows' are explicitly named in the WRAP definition. All static callers are Allegro's wwnd.c/wddraw*.c/wsystem.c window driver. |
| USER32.dll | GetAsyncKeyState | DETERMINISTIC |  | 3 | _update_shifts (allegro) | Raw input sampling. All static call sites are Allegro's input drivers (wkeybd.c _update_shifts/_key_dinput_*, wmouse.c _mouse_directx_*/_mouse_dinput_handle) -- GAME code (player.c/control.c) never calls these directly, it reads Allegro's already-abstracted key_pressed[]/mouse state. These Win32 calls ARE nonetheless the actual raw-input sampling points and are the correct place to intercept/record for deterministic replay, since anything downstream is pure (deterministic) game logic. |
| USER32.dll | GetClassLongA | WRAP |  | 1 | _sys_directx_set_close_button_callback (allegro) | Window handle creation/management -- 'windows' are explicitly named in the WRAP definition. All static callers are Allegro's wwnd.c/wddraw*.c/wsystem.c window driver. |
| USER32.dll | GetClientRect | WRAP |  | 1 | _adjust_window (allegro) | Window handle creation/management -- 'windows' are explicitly named in the WRAP definition. All static callers are Allegro's wwnd.c/wddraw*.c/wsystem.c window driver. |
| USER32.dll | GetCursorPos | DETERMINISTIC |  | 8 | _mouse_directx_set_speed (allegro); _mouse_directx_set_range (allegro); _mouse_directx_position (allegro); _mouse_dinput_handle (allegro) | Raw input sampling. All static call sites are Allegro's input drivers (wkeybd.c _update_shifts/_key_dinput_*, wmouse.c _mouse_directx_*/_mouse_dinput_handle) -- GAME code (player.c/control.c) never calls these directly, it reads Allegro's already-abstracted key_pressed[]/mouse state. These Win32 calls ARE nonetheless the actual raw-input sampling points and are the correct place to intercept/record for deterministic replay, since anything downstream is pure (deterministic) game logic. |
| USER32.dll | GetDC | WRAP |  | 11 | _sys_directx_get_desktop_resolution (allegro); _sys_directx_desktop_color_depth (allegro); _update_colorconv_window (allegro); _update_mouse_pointer (allegro) | DC/paint-cycle handle boundary tied to the window's GDI surface. |
| USER32.dll | GetForegroundWindow | WRAP |  | 2 | __win_switch_out (allegro); _update_colorconv_window (allegro) | Window handle creation/management -- 'windows' are explicitly named in the WRAP definition. All static callers are Allegro's wwnd.c/wddraw*.c/wsystem.c window driver. |
| USER32.dll | GetKeyboardState | DETERMINISTIC |  | 3 | _key_dinput_acquire (allegro); _key_dinput_handle_scancode (allegro) | Raw input sampling. All static call sites are Allegro's input drivers (wkeybd.c _update_shifts/_key_dinput_*, wmouse.c _mouse_directx_*/_mouse_dinput_handle) -- GAME code (player.c/control.c) never calls these directly, it reads Allegro's already-abstracted key_pressed[]/mouse state. These Win32 calls ARE nonetheless the actual raw-input sampling points and are the correct place to intercept/record for deterministic replay, since anything downstream is pure (deterministic) game logic. |
| USER32.dll | GetKeyNameTextA | DETERMINISTIC |  | 1 | _key_directx_scancode_to_name (allegro) | Raw input sampling. All static call sites are Allegro's input drivers (wkeybd.c _update_shifts/_key_dinput_*, wmouse.c _mouse_directx_*/_mouse_dinput_handle) -- GAME code (player.c/control.c) never calls these directly, it reads Allegro's already-abstracted key_pressed[]/mouse state. These Win32 calls ARE nonetheless the actual raw-input sampling points and are the correct place to intercept/record for deterministic replay, since anything downstream is pure (deterministic) game logic. |
| USER32.dll | GetMessageA | DETERMINISTIC |  | 1 | _wnd_thread_proc (allegro) | Message-pump gating: blocks/paces the main loop and is the channel through which real WM_KEYDOWN/WM_MOUSEMOVE/WM_TIMER/WM_SIZE etc. arrive. Loop pacing and event order here affect frame timing and input delivery order, so this needs control for a fully deterministic replay, even though it is Allegro plumbing rather than GAME code. |
| USER32.dll | GetSystemMenu | WRAP |  | 1 | _sys_directx_set_close_button_callback (allegro) | Window handle creation/management -- 'windows' are explicitly named in the WRAP definition. All static callers are Allegro's wwnd.c/wddraw*.c/wsystem.c window driver. |
| USER32.dll | GetSystemMetrics | DIRECT |  | 4 | _mouse_directx_init (allegro); _adjust_window (allegro); _gfx_directx_set_mouse_sprite (allegro) | Host display-metrics query (screen size etc.), pure read, no handle created. |
| USER32.dll | GetWindowLongA | WRAP |  | 3 | _adjust_window (allegro); _init_directx_window (allegro); __win_switch_out (allegro) | Window handle creation/management -- 'windows' are explicitly named in the WRAP definition. All static callers are Allegro's wwnd.c/wddraw*.c/wsystem.c window driver. |
| USER32.dll | GetWindowRect | WRAP |  | 4 | _sys_directx_save_console_state (allegro); _init_directx_window (allegro); _handle_window_move_win (allegro); _move_overlay (allegro) | Window handle creation/management -- 'windows' are explicitly named in the WRAP definition. All static callers are Allegro's wwnd.c/wddraw*.c/wsystem.c window driver. |
| USER32.dll | InvalidateRect | WRAP |  | 1 | _handle_window_move_win (allegro) | DC/paint-cycle handle boundary tied to the window's GDI surface. |
| USER32.dll | IsIconic | WRAP |  | 1 | _directx_wnd_proc@16 (allegro) | Window handle creation/management -- 'windows' are explicitly named in the WRAP definition. All static callers are Allegro's wwnd.c/wddraw*.c/wsystem.c window driver. |
| USER32.dll | KillTimer | WRAP |  | 1 | _directx_wnd_proc@16 (allegro) | Pairs with SetTimer to cancel the timer. |
| USER32.dll | LoadCursorA | WRAP |  | 8 | _main_menu_callback (game); _mouse_directx_select_system_cursor (allegro); _mouse_set_syscursor (allegro); _create_directx_window (allegro) | GDI cursor/icon resource handles -- window-chrome, not gameplay-relevant, but a resource-identity boundary per the WRAP definition. |
| USER32.dll | LoadIconA | WRAP |  | 2 | _create_directx_window (allegro) | GDI cursor/icon resource handles -- window-chrome, not gameplay-relevant, but a resource-identity boundary per the WRAP definition. |
| USER32.dll | MapVirtualKeyA | DETERMINISTIC |  | 1 | _key_dinput_handle_scancode (allegro) | Raw input sampling. All static call sites are Allegro's input drivers (wkeybd.c _update_shifts/_key_dinput_*, wmouse.c _mouse_directx_*/_mouse_dinput_handle) -- GAME code (player.c/control.c) never calls these directly, it reads Allegro's already-abstracted key_pressed[]/mouse state. These Win32 calls ARE nonetheless the actual raw-input sampling points and are the correct place to intercept/record for deterministic replay, since anything downstream is pure (deterministic) game logic. |
| USER32.dll | MessageBoxA | WRAP |  | 2 | __win_thread_init (allegro) | Modal UI that blocks the calling thread on real user input. Static callers found: Allegro's wthread.c (__win_thread_init error path) and (via data_refs on __iob) CRT/Allegro assert/error paths. Blocking on a real human click is a nondeterminism risk for any replay/automation harness, worth flagging even though class is WRAP not DETERMINISTIC. |
| USER32.dll | MessageBoxW | WRAP |  | 1 | _sys_directx_message (allegro) | Modal UI that blocks the calling thread on real user input. Static callers found: Allegro's wthread.c (__win_thread_init error path) and (via data_refs on __iob) CRT/Allegro assert/error paths. Blocking on a real human click is a nondeterminism risk for any replay/automation harness, worth flagging even though class is WRAP not DETERMINISTIC. |
| USER32.dll | MoveWindow | WRAP |  | 1 | _adjust_window (allegro) | Window handle creation/management -- 'windows' are explicitly named in the WRAP definition. All static callers are Allegro's wwnd.c/wddraw*.c/wsystem.c window driver. |
| USER32.dll | MsgWaitForMultipleObjects | DETERMINISTIC |  | 1 | _wnd_thread_proc (allegro) | Message-pump gating: blocks/paces the main loop and is the channel through which real WM_KEYDOWN/WM_MOUSEMOVE/WM_TIMER/WM_SIZE etc. arrive. Loop pacing and event order here affect frame timing and input delivery order, so this needs control for a fully deterministic replay, even though it is Allegro plumbing rather than GAME code. |
| USER32.dll | PeekMessageA | DETERMINISTIC |  | 1 | _wnd_thread_proc (allegro) | Message-pump gating: blocks/paces the main loop and is the channel through which real WM_KEYDOWN/WM_MOUSEMOVE/WM_TIMER/WM_SIZE etc. arrive. Loop pacing and event order here affect frame timing and input delivery order, so this needs control for a fully deterministic replay, even though it is Allegro plumbing rather than GAME code. |
| USER32.dll | PostMessageA | WRAP |  | 5 | _mouse_directx_select_system_cursor (allegro); _mouse_set_syscursor (allegro); _key_dinput_handle_scancode (allegro); _wnd_schedule_proc (allegro) | Message-routing calls; SendMessageA in particular re-enters a WndProc synchronously (same caveat as DispatchMessageA) but has no static caller among the sampled sites beyond Allegro's own window/thread plumbing. |
| USER32.dll | PostQuitMessage | WRAP |  | 1 | _directx_wnd_proc@16 (allegro) | Message-routing calls; SendMessageA in particular re-enters a WndProc synchronously (same caveat as DispatchMessageA) but has no static caller among the sampled sites beyond Allegro's own window/thread plumbing. |
| USER32.dll | RedrawWindow | WRAP |  | 1 | _sys_directx_set_close_button_callback (allegro) | DC/paint-cycle handle boundary tied to the window's GDI surface. |
| USER32.dll | RegisterClassA | WRAP | Y | 1 | _create_directx_window (allegro) | Registers a window class with a WNDPROC pointer (Allegro's own _directx_wnd_proc in wwnd.c) -- textbook callback-bearing import; every subsequent message dispatched to this window re-enters guest (Allegro) code through that pointer. |
| USER32.dll | RegisterWindowMessageA | WRAP |  | 2 | _init_directx_window (allegro) | Message-routing calls; SendMessageA in particular re-enters a WndProc synchronously (same caveat as DispatchMessageA) but has no static caller among the sampled sites beyond Allegro's own window/thread plumbing. |
| USER32.dll | ReleaseDC | WRAP |  | 11 | _sys_directx_get_desktop_resolution (allegro); _sys_directx_desktop_color_depth (allegro); _update_colorconv_window (allegro); _update_mouse_pointer (allegro) | DC/paint-cycle handle boundary tied to the window's GDI surface. |
| USER32.dll | SendMessageA | WRAP |  | 1 | _wnd_call_proc (allegro) | Message-routing calls; SendMessageA in particular re-enters a WndProc synchronously (same caveat as DispatchMessageA) but has no static caller among the sampled sites beyond Allegro's own window/thread plumbing. |
| USER32.dll | SetClassLongA | WRAP |  | 3 | _sys_directx_set_close_button_callback (allegro); _gfx_directx_ovl_exit (allegro); _init_directx_ovl (allegro) | Window handle creation/management -- 'windows' are explicitly named in the WRAP definition. All static callers are Allegro's wwnd.c/wddraw*.c/wsystem.c window driver. |
| USER32.dll | SetCursor | WRAP |  | 5 | _mouse_directx_select_system_cursor (allegro); _mouse_set_syscursor (allegro); _gfx_directx_hide_mouse (allegro); _gfx_directx_show_mouse (allegro) | GDI cursor/icon resource handles -- window-chrome, not gameplay-relevant, but a resource-identity boundary per the WRAP definition. |
| USER32.dll | SetCursorPos | DETERMINISTIC |  | 3 | _mouse_directx_position (allegro); _gfx_directx_hide_mouse (allegro); _gfx_directx_show_mouse (allegro) | Raw input sampling. All static call sites are Allegro's input drivers (wkeybd.c _update_shifts/_key_dinput_*, wmouse.c _mouse_directx_*/_mouse_dinput_handle) -- GAME code (player.c/control.c) never calls these directly, it reads Allegro's already-abstracted key_pressed[]/mouse state. These Win32 calls ARE nonetheless the actual raw-input sampling points and are the correct place to intercept/record for deterministic replay, since anything downstream is pure (deterministic) game logic. |
| USER32.dll | SetForegroundWindow | WRAP |  | 2 | _create_directx_window (allegro); _set_video_mode (allegro) | Window handle creation/management -- 'windows' are explicitly named in the WRAP definition. All static callers are Allegro's wwnd.c/wddraw*.c/wsystem.c window driver. |
| USER32.dll | SetTimer | DETERMINISTIC | Y | 1 | _directx_wnd_proc@16 (allegro) | Textbook callback-bearing import (optional TIMERPROC pointer) and, more importantly, a WM_TIMER-driven time source. Only static caller is Allegro's window proc (_directx_wnd_proc in wwnd.c) with a single call site -- likely a periodic housekeeping/redraw tick rather than the main game clock (that is QueryPerformanceCounter). |
| USER32.dll | SetWindowLongA | WRAP |  | 4 | _restore_window_style (allegro); _exit_directx_window (allegro); _init_directx_window (allegro); _set_video_mode (allegro) | Window handle creation/management -- 'windows' are explicitly named in the WRAP definition. All static callers are Allegro's wwnd.c/wddraw*.c/wsystem.c window driver. |
| USER32.dll | SetWindowPos | WRAP |  | 4 | _sys_directx_restore_console_state (allegro); _restore_window_style (allegro); _handle_window_move_win (allegro); _move_overlay (allegro) | Window handle creation/management -- 'windows' are explicitly named in the WRAP definition. All static callers are Allegro's wwnd.c/wddraw*.c/wsystem.c window driver. |
| USER32.dll | SetWindowTextA | WRAP |  | 1 | _sys_directx_set_window_title (allegro) | Window handle creation/management -- 'windows' are explicitly named in the WRAP definition. All static callers are Allegro's wwnd.c/wddraw*.c/wsystem.c window driver. |
| USER32.dll | ShowWindow | WRAP |  | 3 | _sys_directx_restore_console_state (allegro); _create_directx_window (allegro); _set_video_mode (allegro) | Window handle creation/management -- 'windows' are explicitly named in the WRAP definition. All static callers are Allegro's wwnd.c/wddraw*.c/wsystem.c window driver. |
| USER32.dll | SystemParametersInfoA | DIRECT |  | 2 | _adjust_window (allegro); _T.189 (allegro) | Pure query/conversion of host state with no persistent resource created; safe to forward as-is. (GetVersion/GetVersionExA will report Win11's compatibility-shimmed version, which is a host quirk, not a carrier concern.) |
| USER32.dll | ToAscii | DETERMINISTIC |  | 1 | _key_dinput_handle_scancode (allegro) | Raw input sampling. All static call sites are Allegro's input drivers (wkeybd.c _update_shifts/_key_dinput_*, wmouse.c _mouse_directx_*/_mouse_dinput_handle) -- GAME code (player.c/control.c) never calls these directly, it reads Allegro's already-abstracted key_pressed[]/mouse state. These Win32 calls ARE nonetheless the actual raw-input sampling points and are the correct place to intercept/record for deterministic replay, since anything downstream is pure (deterministic) game logic. |
| USER32.dll | UpdateWindow | WRAP |  | 1 | _create_directx_window (allegro) | Window handle creation/management -- 'windows' are explicitly named in the WRAP definition. All static callers are Allegro's wwnd.c/wddraw*.c/wsystem.c window driver. |
| WINMM.DLL | joyGetDevCapsA | WRAP |  | 1 | _joystick_win32_init (allegro) | Joystick device enumeration/capability query, not per-frame -- Allegro's wjoyw32.c fallback driver. |
| WINMM.DLL | joyGetNumDevs | WRAP |  | 1 | _joystick_win32_init (allegro) | Joystick device enumeration/capability query, not per-frame -- Allegro's wjoyw32.c fallback driver. |
| WINMM.DLL | joyGetPosEx | DETERMINISTIC |  | 2 | _joystick_win32_poll (allegro); _joystick_win32_init (allegro) | Per-poll joystick position read (input). Only static caller is Allegro's wjoyw32.c (_joystick_win32_poll/_init) -- legacy WinMM joystick fallback driver used when DirectInput joystick isn't available; feeds the same abstracted joystick state GAME code (control.c's _get_gamepad et al.) reads indirectly. |
| WINMM.DLL | midiInClose | WRAP |  | 1 | _midi_win32_in_exit (allegro) | MIDI device I/O, still natively supported on Windows 11 (no shim needed) -- device-handle identity/state boundary, Allegro's midi.c driver. |
| WINMM.DLL | midiInGetDevCapsA | WRAP |  | 1 | __get_win_midi_driver_list (allegro) | MIDI device I/O, still natively supported on Windows 11 (no shim needed) -- device-handle identity/state boundary, Allegro's midi.c driver. |
| WINMM.DLL | midiInGetNumDevs | WRAP |  | 1 | __get_win_midi_driver_list (allegro) | MIDI device I/O, still natively supported on Windows 11 (no shim needed) -- device-handle identity/state boundary, Allegro's midi.c driver. |
| WINMM.DLL | midiInOpen | WRAP | Y | 1 | _midi_win32_in_init (allegro) | Opens a MIDI device with an optional callback (function pointer or window/thread handle) for input/completion notification -- callback-bearing. |
| WINMM.DLL | midiInReset | WRAP |  | 2 | _midi_win32_in_exit (allegro); _midi_win32_in_init (allegro) | MIDI device I/O, still natively supported on Windows 11 (no shim needed) -- device-handle identity/state boundary, Allegro's midi.c driver. |
| WINMM.DLL | midiInStart | WRAP |  | 1 | _midi_win32_in_init (allegro) | MIDI device I/O, still natively supported on Windows 11 (no shim needed) -- device-handle identity/state boundary, Allegro's midi.c driver. |
| WINMM.DLL | midiInStop | WRAP |  | 1 | _midi_win32_in_exit (allegro) | MIDI device I/O, still natively supported on Windows 11 (no shim needed) -- device-handle identity/state boundary, Allegro's midi.c driver. |
| WINMM.DLL | midiOutClose | WRAP |  | 1 | _midi_win32_exit (allegro) | MIDI device I/O, still natively supported on Windows 11 (no shim needed) -- device-handle identity/state boundary, Allegro's midi.c driver. |
| WINMM.DLL | midiOutGetDevCapsA | WRAP |  | 1 | __get_win_midi_driver_list (allegro) | MIDI device I/O, still natively supported on Windows 11 (no shim needed) -- device-handle identity/state boundary, Allegro's midi.c driver. |
| WINMM.DLL | midiOutGetNumDevs | WRAP |  | 1 | __get_win_midi_driver_list (allegro) | MIDI device I/O, still natively supported on Windows 11 (no shim needed) -- device-handle identity/state boundary, Allegro's midi.c driver. |
| WINMM.DLL | midiOutGetVolume | WRAP |  | 1 | _midi_win32_get_mixer_volume (allegro) | MIDI device I/O, still natively supported on Windows 11 (no shim needed) -- device-handle identity/state boundary, Allegro's midi.c driver. |
| WINMM.DLL | midiOutOpen | WRAP | Y | 1 | _midi_win32_init (allegro) | Opens a MIDI device with an optional callback (function pointer or window/thread handle) for input/completion notification -- callback-bearing. |
| WINMM.DLL | midiOutReset | WRAP |  | 4 | _midi_win32_raw_midi (allegro); _midi_switch_out (allegro); _midi_win32_exit (allegro); _midi_win32_init (allegro) | MIDI device I/O, still natively supported on Windows 11 (no shim needed) -- device-handle identity/state boundary, Allegro's midi.c driver. |
| WINMM.DLL | midiOutSetVolume | WRAP |  | 1 | _midi_win32_set_mixer_volume (allegro) | MIDI device I/O, still natively supported on Windows 11 (no shim needed) -- device-handle identity/state boundary, Allegro's midi.c driver. |
| WINMM.DLL | midiOutShortMsg | WRAP |  | 1 | _midi_win32_raw_midi (allegro) | MIDI device I/O, still natively supported on Windows 11 (no shim needed) -- device-handle identity/state boundary, Allegro's midi.c driver. |
| WINMM.DLL | timeGetTime | DETERMINISTIC |  | 5 | _tim_win32_low_perf_thread (allegro); _tim_win32_rest (allegro) | Millisecond time source. All static call sites are Allegro's wtimer.c low-perf timer thread (_tim_win32_low_perf_thread/_tim_win32_rest) -- Allegro plumbing, not called directly by GAME code (which uses QueryPerformanceCounter instead, see above). |
| WINMM.DLL | waveOutClose | WRAP |  | 1 | _digi_waveout_exit (allegro) | Waveform-audio device/buffer I/O, still natively supported on Win11 -- device-handle and audio-buffer identity boundary (Allegro's wsndwo.c waveOut digital sound driver). |
| WINMM.DLL | waveOutGetPosition | WRAP |  | 1 | _digi_waveout_mixer_callback (allegro) | Waveform-audio device/buffer I/O, still natively supported on Win11 -- device-handle and audio-buffer identity boundary (Allegro's wsndwo.c waveOut digital sound driver). |
| WINMM.DLL | waveOutGetVolume | WRAP |  | 2 | _digi_waveout_get_mixer_volume (allegro); _digi_waveout_init (allegro) | Waveform-audio device/buffer I/O, still natively supported on Win11 -- device-handle and audio-buffer identity boundary (Allegro's wsndwo.c waveOut digital sound driver). |
| WINMM.DLL | waveOutOpen | WRAP | Y | 1 | _digi_waveout_init (allegro) | Opens a waveOut device with an optional callback (function/window/thread/event) for buffer-completion notification -- callback-bearing. |
| WINMM.DLL | waveOutPause | WRAP |  | 1 | _digi_waveout_mixer_callback (allegro) | Waveform-audio device/buffer I/O, still natively supported on Win11 -- device-handle and audio-buffer identity boundary (Allegro's wsndwo.c waveOut digital sound driver). |
| WINMM.DLL | waveOutPrepareHeader | WRAP |  | 1 | _digi_waveout_init (allegro) | Waveform-audio device/buffer I/O, still natively supported on Win11 -- device-handle and audio-buffer identity boundary (Allegro's wsndwo.c waveOut digital sound driver). |
| WINMM.DLL | waveOutReset | WRAP |  | 1 | _digi_waveout_exit (allegro) | Waveform-audio device/buffer I/O, still natively supported on Win11 -- device-handle and audio-buffer identity boundary (Allegro's wsndwo.c waveOut digital sound driver). |
| WINMM.DLL | waveOutRestart | WRAP |  | 1 | _digi_waveout_mixer_callback (allegro) | Waveform-audio device/buffer I/O, still natively supported on Win11 -- device-handle and audio-buffer identity boundary (Allegro's wsndwo.c waveOut digital sound driver). |
| WINMM.DLL | waveOutSetVolume | WRAP |  | 2 | _digi_waveout_set_mixer_volume (allegro); _digi_waveout_exit (allegro) | Waveform-audio device/buffer I/O, still natively supported on Win11 -- device-handle and audio-buffer identity boundary (Allegro's wsndwo.c waveOut digital sound driver). |
| WINMM.DLL | waveOutUnprepareHeader | WRAP |  | 1 | _digi_waveout_exit (allegro) | Waveform-audio device/buffer I/O, still natively supported on Win11 -- device-handle and audio-buffer identity boundary (Allegro's wsndwo.c waveOut digital sound driver). |
| WINMM.DLL | waveOutWrite | WRAP |  | 1 | _digi_waveout_init (allegro) | Waveform-audio device/buffer I/O, still natively supported on Win11 -- device-handle and audio-buffer identity boundary (Allegro's wsndwo.c waveOut digital sound driver). |
| WSOCK32.DLL | closesocket | WRAP |  | 1 | _HTTPFetchInternal (game) | Socket/protocol setup and error-query calls, all from GAME's _HTTPFetchInternal (httpget.c) -- non-data-bearing plumbing around the actual network I/O below. |
| WSOCK32.DLL | connect | DETERMINISTIC |  | 1 | _HTTPFetchInternal (game) | Actual network I/O -- explicitly named in the task's DETERMINISTIC/network category. KNOWN: the sole caller of every one of these is GAME code (_HTTPFetchInternal in httpget.c), used by the ad-banner subsystem (fld_adspot.c) to fetch remote ad images/config, not by core gameplay/scoring logic (hisc.c's own high-score code has no network calls in this census). Content/timing/failure of these calls should NOT be allowed to affect gameplay determinism; recommend stubbing/sandboxing the whole ad-fetch path for replay purposes rather than trying to make live network I/O deterministic. |
| WSOCK32.DLL | gethostbyname | DETERMINISTIC |  | 1 | _HTTPFetchInternal (game) | Actual network I/O -- explicitly named in the task's DETERMINISTIC/network category. KNOWN: the sole caller of every one of these is GAME code (_HTTPFetchInternal in httpget.c), used by the ad-banner subsystem (fld_adspot.c) to fetch remote ad images/config, not by core gameplay/scoring logic (hisc.c's own high-score code has no network calls in this census). Content/timing/failure of these calls should NOT be allowed to affect gameplay determinism; recommend stubbing/sandboxing the whole ad-fetch path for replay purposes rather than trying to make live network I/O deterministic. |
| WSOCK32.DLL | htons | WRAP |  | 1 | _HTTPFetchInternal (game) | Socket/protocol setup and error-query calls, all from GAME's _HTTPFetchInternal (httpget.c) -- non-data-bearing plumbing around the actual network I/O below. |
| WSOCK32.DLL | recv | DETERMINISTIC |  | 1 | _HTTPFetchInternal (game) | Actual network I/O -- explicitly named in the task's DETERMINISTIC/network category. KNOWN: the sole caller of every one of these is GAME code (_HTTPFetchInternal in httpget.c), used by the ad-banner subsystem (fld_adspot.c) to fetch remote ad images/config, not by core gameplay/scoring logic (hisc.c's own high-score code has no network calls in this census). Content/timing/failure of these calls should NOT be allowed to affect gameplay determinism; recommend stubbing/sandboxing the whole ad-fetch path for replay purposes rather than trying to make live network I/O deterministic. |
| WSOCK32.DLL | send | DETERMINISTIC |  | 1 | _HTTPFetchInternal (game) | Actual network I/O -- explicitly named in the task's DETERMINISTIC/network category. KNOWN: the sole caller of every one of these is GAME code (_HTTPFetchInternal in httpget.c), used by the ad-banner subsystem (fld_adspot.c) to fetch remote ad images/config, not by core gameplay/scoring logic (hisc.c's own high-score code has no network calls in this census). Content/timing/failure of these calls should NOT be allowed to affect gameplay determinism; recommend stubbing/sandboxing the whole ad-fetch path for replay purposes rather than trying to make live network I/O deterministic. |
| WSOCK32.DLL | setsockopt | WRAP |  | 1 | _HTTPFetchInternal (game) | Socket/protocol setup and error-query calls, all from GAME's _HTTPFetchInternal (httpget.c) -- non-data-bearing plumbing around the actual network I/O below. |
| WSOCK32.DLL | socket | WRAP |  | 1 | _HTTPFetchInternal (game) | Socket/protocol setup and error-query calls, all from GAME's _HTTPFetchInternal (httpget.c) -- non-data-bearing plumbing around the actual network I/O below. |
| WSOCK32.DLL | WSAGetLastError | WRAP |  | 1 | _getSocketError (game) | Socket/protocol setup and error-query calls, all from GAME's _HTTPFetchInternal (httpget.c) -- non-data-bearing plumbing around the actual network I/O below. |
| WSOCK32.DLL | WSAStartup | WRAP |  | 1 | _init_game (game) | Winsock negotiation, called once from GAME's _init_game (main.c) unconditionally at startup -- one-time handshake, no data-dependent effect on replay by itself. |

## The game's true OS surface (imports called directly from GAME code, not just Allegro/CRT/libvorbis)

KNOWN, from disassembly: **102** of 320 imports have at least one call site or direct data reference inside icytower's own source (`F:\projects\icytower\trunk\source\*.c`), as opposed to only being reached through Allegro's platform-abstraction layer, the CRT startup glue, or the statically-linked libvorbis/libogg codec. This is the surface a carrier must get *exactly right*, since it is code the game itself wrote, not third-party plumbing that can be swapped wholesale for a modern equivalent.

- **KERNEL32.dll** (3): `LoadLibraryA`, `QueryPerformanceCounter`, `QueryPerformanceFrequency`
- **libpng3.dll** (35): `png_create_info_struct`, `png_create_read_struct`, `png_create_write_struct`, `png_destroy_read_struct`, `png_destroy_write_struct`, `png_error`, `png_get_gAMA`, `png_get_IHDR`, `png_get_io_ptr`, `png_get_PLTE`, `png_get_rowbytes`, `png_get_sRGB`, `png_get_valid`, `png_read_end`, `png_read_info`, `png_read_row`, `png_read_update_info`, `png_set_bgr`, `png_set_compression_level`, `png_set_expand`, `png_set_gamma`, `png_set_gray_to_rgb`, `png_set_IHDR`, `png_set_interlace_handling`, `png_set_packing`, `png_set_PLTE`, `png_set_read_fn`, `png_set_sig_bytes`, `png_set_strip_16`, `png_set_tRNS_to_alpha`, `png_set_write_fn`, `png_sig_cmp`, `png_write_end`, `png_write_info`, `png_write_row`
- **msvcrt.dll** (49): `__mb_cur_max`, `_chdir`, `_errno`, `_getcwd`, `_isctype`, `_mkdir`, `_pctype`, `_rmdir`, `_setjmp`, `_stat`, `_strdup`, `_stricmp`, `_strnicmp`, `_tzname`, `_tzset`, `atof`, `clock`, `exit`, `fclose`, `fgets`, `fopen`, `fprintf`, `fputc`, `fputs`, `fread`, `free`, `fwrite`, `getenv`, `gmtime`, `localtime`, `malloc`, `mktime`, `printf`, `qsort`, `rand`, `realloc`, `sprintf`, `srand`, `sscanf`, `strcat`, `strchr`, `strcmp`, `strcpy`, `strncmp`, `strncpy`, `strtol`, `time`, `vfprintf`, `vsprintf`
- **pthreadGC2.dll** (3): `pthread_create`, `pthread_mutex_lock`, `pthread_mutex_unlock`
- **SHELL32.DLL** (1): `ShellExecuteA`
- **USER32.dll** (1): `LoadCursorA`
- **WSOCK32.DLL** (10): `closesocket`, `connect`, `gethostbyname`, `htons`, `recv`, `send`, `setsockopt`, `socket`, `WSAGetLastError`, `WSAStartup`

Notes:
- `libpng3.dll` (35 of the 102) is a bundled third-party DLL, not a Windows OS component -- it inflates this count but isn't part of the "forward to host Windows" surface at all; it should just keep loading as a normal DLL dependency.
- Excluding libpng3.dll, the game's real Win32 OS surface is **67 imports**: 3x KERNEL32 (`LoadLibraryA`, `QueryPerformanceCounter`, `QueryPerformanceFrequency`), 1x SHELL32 (`ShellExecuteA`), 1x USER32 (`LoadCursorA`), 10x WSOCK32 (all of it, the ad-fetch HTTP client), 49x msvcrt.dll (file I/O, CRT/heap/string/math/time, `rand`/`srand`, `qsort`), and 3x pthreadGC2.dll (the ad-fetch worker thread).
- The single most consequential entries here for a *deterministic* carrier are `QueryPerformanceCounter` (core game-loop timing, called from `_play` in main.c) and `rand`/`srand` (per-run gameplay RNG, called from `_new_game`/`_init_game`/`_create_post`) -- see the Determinism Boundary section below.

## COM boundary (not visible in the static import table)

**KNOWN, structural point:** `DirectDrawCreate`, `DirectInputCreateA`, and `DirectSoundCreate` are only the *factory* calls -- they appear once each in the import table and are each called from exactly one place in Allegro's driver init code. Every actual frame of work (surface blit/lock, sound-buffer write, keyboard/mouse/joystick poll) happens through **virtual-call dispatch on the COM object these factories return**, which is *not* an import and therefore invisible to any import-table census, static or dynamic-loader-based. A carrier that only forwards the import table will correctly create the DirectX objects but will not see (and cannot intercept at the import level) any of the real per-frame traffic through them.

**INFERRED (estimated) method identity**, from scanning `call *OFFSET(%reg)` instructions (the standard 32-bit MSVC/MinGW COM vtable-call idiom) inside the specific Allegro source files that own each COM interface, and matching the byte offsets against the well-known IUnknown-prefixed (QueryInterface=0x0, AddRef=0x4, Release=0x8) vtable layouts of `IDirectDraw2`, `IDirectDrawSurface`, `IDirectSound`, `IDirectSoundBuffer`, `IDirectSoundCaptureBuffer`, and `IDirectInputDeviceA`/`IDirectInputDevice2A` from the classic DirectX 7-era SDK headers:

| Allegro source file | inferred interface | vtable-call sites found | offsets -> inferred method |
|---|---|---:|---|
| wddraw.c | IDirectDraw2 / IDirectDrawSurface (primary surf. palette) | 16 | 0x8=Release(IUnknown), 0x10=CreateClipper, 0x14=CreatePalette, 0x18=CreateSurface, 0x20=EnumDisplayModes, 0x2c=GetCaps, 0x34=GetFourCCCodes, 0x3c=GetMonitorFrequency, 0x50=SetCooperativeLevel, 0x58=WaitForVerticalBlank, 0x5c=GetAvailableVidMem, 0x7c=SetPalette |
| wddaccel.c | IDirectDrawSurface | 16 | 0x14=Blt, 0x1c=BltFast, 0x48=GetFlipStatus, 0x74=SetColorKey |
| wddbmp.c | IDirectDrawSurface | 15 | 0x8=Release(IUnknown), 0x18=BltBatch, 0x2c=Flip, 0x30=GetAttachedSurface, 0x44=GetDC, 0x48=GetFlipStatus, 0x68=ReleaseDC, 0x7c=SetPalette |
| wddbmpl.c | IDirectDrawSurface | 1 | 0x6c=Restore |
| wddfull.c | _(no offset-call pattern found)_ | 0 | |
| wddwin.c | IDirectDrawSurface | 10 | 0x14=Blt, 0x44=GetDC, 0x50=GetPalette, 0x60=IsLost, 0x64=Lock, 0x68=ReleaseDC, 0x70=SetClipper, 0x80=Unlock |
| wddovl.c | IDirectDrawSurface (overlay) | 7 | 0x2c=Flip, 0x50=GetPalette, 0x60=IsLost, 0x74=SetColorKey, 0x84=UpdateOverlay |
| wddmode.c | IDirectDrawSurface | 5 | 0x20=DeleteAttachedSurface, 0x30=GetAttachedSurface, 0x50=GetPalette, 0x54=GetPixelFormat |
| wddlock.c | IDirectDrawSurface | 6 | 0x64=Lock, 0x80=Unlock |
| wdsound.c | IDirectSound / IDirectSoundBuffer | 45 | 0x8=Release(IUnknown), 0xc=GetCaps, 0x10=GetCurrentPosition, 0x14=GetFormat, 0x18=GetVolume, 0x24=GetStatus, 0x2c=Lock, 0x30=Play, 0x34=SetCurrentPosition, 0x38=SetFormat, 0x3c=SetVolume, 0x40=SetPan, 0x44=SetFrequency, 0x48=Stop, 0x4c=Unlock, 0x50=Restore |
| wdsndmix.c | IDirectSoundBuffer (software mixer) | 26 | 0x8=Release(IUnknown), 0xc=GetCaps, 0x10=GetCurrentPosition, 0x14=GetFormat, 0x18=GetVolume, 0x24=GetStatus, 0x2c=Lock, 0x30=Play, 0x38=SetFormat, 0x3c=SetVolume, 0x48=Stop, 0x4c=Unlock, 0x50=Restore |
| wdsinput.c | IDirectSoundCaptureBuffer | 18 | 0x8=Release(IUnknown), 0xc=GetCaps, 0x10=GetCurrentPosition, 0x14=GetFormat, 0x20=Lock, 0x24=Start, 0x28=Stop, 0x2c=Unlock |
| wkeybd.c | IDirectInputDeviceA (keyboard) | 10 | 0x8=Release(IUnknown), 0xc=GetCapabilities, 0x18=SetProperty, 0x1c=Acquire, 0x20=Unacquire, 0x28=GetDeviceData, 0x2c=SetDataFormat, 0x30=SetEventNotification, 0x34=SetCooperativeLevel |
| wmouse.c | IDirectInputDeviceA (mouse) | 11 | 0x8=Release(IUnknown), 0xc=GetCapabilities, 0x10=EnumObjects, 0x18=SetProperty, 0x1c=Acquire, 0x20=Unacquire, 0x28=GetDeviceData, 0x2c=SetDataFormat, 0x30=SetEventNotification, 0x34=SetCooperativeLevel |
| wjoydx.c | IDirectInputDevice(2)A (joystick) | 16 | 0x8=Release(IUnknown), 0xc=GetCapabilities, 0x10=EnumObjects, 0x18=SetProperty, 0x1c=Acquire, 0x20=Unacquire, 0x24=GetDeviceState, 0x2c=SetDataFormat, 0x34=SetCooperativeLevel, 0x64=Poll(IDirectInputDevice2) |
| winput.c | _(no offset-call pattern found)_ | 0 | |

Caveats (all of the above vtable-offset mapping is **INFERRED**, not verified against a known-good DirectX header for this exact build): offsets are read straight off `call *0xNN(%eax)`-style instructions in the disassembly (KNOWN), but the method name at each offset is my recollection of the classic (non-COM-idl-regenerated) DirectX 7 SDK vtable layout, not cross-checked against `dxguid`/`ddraw.h` structure definitions from this codebase. The wjoydx.c offset `0x64` (`Escape`) implies `IDirectInputDevice2A`, not the plain `IDirectInputDeviceA` used by keyboard/mouse -- plausible since force-feedback/vendor-escape calls are joystick-specific, but unverified. `IDirectDrawClipper` and `IDirectDrawPalette` objects are created (`CreateClipper`/`CreatePalette`, both visible as `IDirectDraw2` calls above) but their own small vtables were not separately identified as distinct call clusters in this pass.

Practical implication for the carrier: forwarding the 4 SHIM factory imports (`DirectDrawCreate`, `DirectInputCreateA`, `DirectSoundCreate`, and indirectly `DirectSoundEnumerateA`) is necessary but nowhere near sufficient -- the carrier must also either (a) forward the *entire* COM object (proxy every vtable slot to the real host DirectX/cnc-ddraw implementation), or (b) replace the object with a compatible reimplementation exposing the ~15-45 methods per interface enumerated above.

## Determinism boundary: minimal interception set for replayable GAME logic

Goal: the smallest set of interception points such that the *game's own logic* -- as opposed to Allegro's plumbing, which can be left non-deterministic as long as it still delivers the same eventual results to the game -- becomes replayable.

**KNOWN, from disassembly (GAME code calls these Win32/CRT imports directly):**

1. **`KERNEL32.dll!QueryPerformanceCounter`** -- called directly from `_play` (main.c), the core game loop, 9 static call sites split between GAME and Allegro. This is the frame/physics clock. **Must be virtualized to a recorded/replayed logical clock** for the game's own per-frame delta-time computation to be reproducible; Allegro's separate use of it (wtimer.c) can ride along on the same virtual clock or be left alone since it only affects audio mixing cadence, not game state.
2. **`msvcrt.dll!rand` / `msvcrt.dll!srand`** -- called directly from `_new_game`/`_init_game` (main.c) and `_create_post` (beta.c). This is the game's own gameplay PRNG (msvcrt's LCG, not a game-authored one). **Must intercept `srand`'s seed value (record it) and either forward to the exact same msvcrt `rand()` algorithm on every host, or replace both with a bundled, version-pinned PRNG** so replay does not depend on which Windows version's msvcrt.dll happens to be forwarded to.
3. **Raw input sampling** (`USER32.dll!GetAsyncKeyState`, `GetKeyboardState`, `GetCursorPos`, `SetCursorPos`; `WINMM.DLL!joyGetPosEx`) -- KNOWN: none of these are called by GAME code directly; all are Allegro driver internals (wkeybd.c/wmouse.c/wjoydx.c). Nonetheless they are the literal raw-input sample points feeding Allegro's abstracted key/mouse/joystick state, which GAME code (player.c/control.c) then reads as pure (deterministic) memory. **Recording/replaying at this exact boundary is sufficient** -- there is no need to go lower (e.g. into DirectInput's `IDirectInputDeviceA::GetDeviceData`/`GetDeviceState` vtable calls, see COM section) unless Allegro's own internal event ordering/timing between WM_* messages and these polls turns out to matter.
4. **Message-pump gating** (`USER32.dll!GetMessageA`/`PeekMessageA`/`MsgWaitForMultipleObjects`) -- Allegro plumbing that paces the main loop and delivers WM_KEYDOWN/WM_SIZE/WM_TIMER etc. Needed only if replay must reproduce exact frame pacing under contention (e.g. window defocus, OS-level message storms); for pure logic-replay given a recorded input+clock+RNG stream, this layer does not need separate control.
5. **`WSOCK32.DLL` (all 10 imports) and `pthreadGC2.dll!pthread_create`** -- KNOWN: sole static caller is `_HTTPFetchInternal`/`_fldads_start` (httpget.c/fld_adspot.c), the ad-banner subsystem. **Not part of core gameplay determinism** -- hisc.c's high-score logic has no network calls in this census. Recommended treatment: stub/sandbox the entire ad-fetch path for replay/testing rather than trying to make live network I/O deterministic.
6. **`msvcrt.dll!time`/`clock`/`localtime`/`gmtime`/`mktime`** -- used for timestamping (save/profile/hiscore/replay metadata), not for per-frame logic; needs virtualizing for byte-identical save files but not for gameplay replay correctness per se.

**Conclusion (INFERRED synthesis of the above KNOWN facts):** the minimal interception set for *replayable game logic* is exactly **{QueryPerformanceCounter, srand-seed-capture, rand-algorithm-pinning, raw keyboard/mouse/joystick input sampling}** -- 3 Win32 imports plus the 2 CRT RNG imports plus a version-pinning decision, five to six intercepts in total. Everything else in the DETERMINISTIC class (Sleep, WaitFor*, SetTimer, _beginthread/pthread_create, the message pump, network I/O) governs Allegro's internal plumbing or the non-gameplay ad subsystem and can be left to run non-deterministically (wall-clock real time, real thread scheduling, real network) as long as the four items above are pinned, since none of that plumbing's timing is read back into game state -- it only decides *when* the next deterministic frame gets computed, not *what* it computes.

Full DETERMINISTIC-class list for reference:

- `KERNEL32.dll!QueryPerformanceCounter` -- origins: allegro, game
- `KERNEL32.dll!QueryPerformanceFrequency` -- origins: allegro, game
- `KERNEL32.dll!Sleep` -- origins: allegro, crt
- `KERNEL32.dll!WaitForMultipleObjects` -- origins: allegro
- `KERNEL32.dll!WaitForSingleObject` -- origins: allegro
- `msvcrt.dll!_beginthread` (callback-bearing) -- origins: allegro
- `msvcrt.dll!clock` -- origins: allegro, game
- `msvcrt.dll!gmtime` -- origins: game
- `msvcrt.dll!localtime` -- origins: game
- `msvcrt.dll!mktime` -- origins: game
- `msvcrt.dll!rand` -- origins: game
- `msvcrt.dll!srand` -- origins: game
- `msvcrt.dll!time` -- origins: game
- `pthreadGC2.dll!pthread_create` (callback-bearing) -- origins: game
- `USER32.dll!GetAsyncKeyState` -- origins: allegro
- `USER32.dll!GetCursorPos` -- origins: allegro
- `USER32.dll!GetKeyboardState` -- origins: allegro
- `USER32.dll!GetKeyNameTextA` -- origins: allegro
- `USER32.dll!GetMessageA` -- origins: allegro
- `USER32.dll!MapVirtualKeyA` -- origins: allegro
- `USER32.dll!MsgWaitForMultipleObjects` -- origins: allegro
- `USER32.dll!PeekMessageA` -- origins: allegro
- `USER32.dll!SetCursorPos` -- origins: allegro
- `USER32.dll!SetTimer` (callback-bearing) -- origins: allegro
- `USER32.dll!ToAscii` -- origins: allegro
- `WINMM.DLL!joyGetPosEx` -- origins: allegro
- `WINMM.DLL!timeGetTime` -- origins: allegro
- `WSOCK32.DLL!connect` -- origins: game
- `WSOCK32.DLL!gethostbyname` -- origins: game
- `WSOCK32.DLL!recv` -- origins: game
- `WSOCK32.DLL!send` -- origins: game

## GetProcAddress caveat (dynamic-linking blind spot)

**KNOWN:** `KERNEL32.dll!GetProcAddress` is itself imported (1 slot, WRAP class). **INFERRED:** whatever the guest resolves through it at runtime is by definition invisible to this or any static import census -- a carrier that only proxies the statically-declared IAT will miss any late-bound optional-API usage. No evidence was collected in this pass about which symbols (if any) are actually looked up through it; recommend a runtime hook/log on `GetProcAddress` before considering the import surface fully characterized.
