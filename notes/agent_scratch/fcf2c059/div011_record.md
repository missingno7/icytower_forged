- 2026-09-08 DIVERGENCE 011 (carrier/NOTES.md, src/icytower/INVIVO.md, PROMOTIONS.md batch-14 addendum): batch 13's flagged "run-to-exit hang/qualification after the tick loop" diagnosed — **not a bug in any of batch 14's sixteen functions**, all of which are correct as recovered and are now all verified in vivo. **Mechanism, and it is divergence 008 at three much larger facilities**: `carrier/gen/pf_bindings_src.h` binds game globals/functions to guest addresses and a hand-curated few CRT names to the guest's IAT slots; everything else clean src/ calls resolves to the CARRIER's own statically linked CRT (carrier.exe is `/BASE:0x10000000`, which is what the crash stacks named). (1) HEAP: `malloc/calloc/realloc/free` were unbound, and in `--det` those four slots are `det.cpp`'s fixed-address arena — `destroy_replay=src` (frees `r`/`r->data` from the still-ORIGINAL `create_replay`) and `save_profile=src` (frees the four buffers `profile_data_page_*` returned) each handed an ARENA pointer to the carrier's UCRT `HeapFree`: STATUS_HEAP_CORRUPTION 0xc0000374, reproducibly, both at the same point (`log.txt` stops after `saving replay: .../last_game.itr`, never reaches `saving config and scores`). The reverse direction leaks silently instead (`arena_count_free_foreign`). (2) CLOCK, and this one fails SILENT: `time`/`clock` were unbound while `QueryPerformanceCounter` was already bound by batch 12 — one third of play()'s three-clock anti-cheat instrument pointed at the guest and two thirds at the host. MEASURED with the heap fixed and the clock not: `save_profile=src` completed normally and wrote `Last updated: 2026-09-08` (real host date) where the original wrote the det-pinned `2026-09-07`; no crash, no digest change, and `tc_c_data`/`tc_t_data` are HASHED into every saved `.itr`. Live in every `play=src` run since batch 12 (13 `time(NULL)` + 3 `clock()` sites in play.c). (3) STDIO: `save_profile` hands the `FILE *` it opens to the still-ORIGINAL `save_control`, so the whole `fopen/fclose/fwrite/fprintf/fputs/fputc/vfprintf` family must share the guest's stream table. **Fix** (carrier/gen/gen_bindings.py only — no src/ edit): 13 new GUEST_CRT_IMPORTS entries, `FILE*` spelled `void*`; `<stdio.h>`/`<stdarg.h>`/`<time.h>` added to GUEST_CRT_PRE_INCLUDES so the macros rewrite src/'s calls and not the headers' own declarations. `sprintf`/`vsprintf`/`printf` deliberately left carrier-side (caller-owned buffers; log2file's byte-identical log.txt is the standing evidence). **New standing check, the durable part**: *every import the carrier WRAPS is an import src/ must call through* — `gen_bindings.py` now PARSES `carrier/src/wrappers.cpp`'s own `kWrapTable` (one list, no copy to rot) and FAILS THE BUILD if src/ calls a wrapped name GUEST_CRT_IMPORTS does not bind. It deliberately does not skip RESERVED_CRT_WINDOWS_IDENTS (that exclusion is exactly what classified malloc/free/time/clock as "expected noise" for three batches) and it scans CALL SITES rather than bare identifiers — needed: the identifier version flagged `exit`, which src/ never calls (prose, plus Allegro's own `GFX_DRIVER.exit` vtable member, which binding would have turned into a syntax error). **New tool**: `carrier/scripts/compare_itr.py` — a byte oracle for a game-written `.itr` that knows save_replay's on-disk write order and masks exactly one column, `tc_s_data` (play.c 3641's `voice_get_position()` music-sync channel: two UNBOUND runs differ in exactly six bytes, all there, and it is one of the two columns calc_replay_checksum does not hash). **Verdicts**: all sixteen bound, individually by file and all at once with every earlier row and `play` — human_test run to the game's own exit gives per-tick digest EQUAL 2293 vs the stored baseline, score 2386 / floor 100, `Done...`, a byte-identical frame stream (2752 frames), and an `assets/` tree byte-identical to an unbound run's except the two `.itr` files the run itself writes, both EQUAL with checksum 0x8180e34d; newgame EQUAL 876 ticks / 982 frames; the `.itr` workload EQUAL 157 ticks with a byte-identical asset tree; gates G1-G5b all EQUAL. Batch 15's concurrently-added create_replay/load_replay are not in the bindfiles (that pass's own functions) though play=src drags them in; four untracked in-flight batch-15 files that do not compile yet (alert.c, game_data.c, menu_keys.c, results.c) are temporarily blocked in build_blockers.json/scan_exclude with the removal condition recorded.
